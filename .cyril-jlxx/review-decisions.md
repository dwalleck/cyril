# PR 116 review decisions

Input: `code-review-pr-116.md`, review of `ffa08676`. Assessment baseline: `1136dabd`; its prior version-pipe and Free+Replace fixes are not reapplied as new work. Finding IDs below are the report IDs, distinct from design claim IDs.

All 54 original records are inventoried, including below-cap, cleanup and refuted candidates; Fgroup records the final review's additional decoder defect. `Verified` establishes the mechanism. Current Linux execution is recorded below and in the implementation checkpoints; native-platform claims require their named CI/acceptance gate.

Historical evidence is preserved separately from new execution proof. Native Windows and entitled-account cloud configuration are not proved by Linux fixtures. Exact-version qualification stays mandatory; child environment replacement is not an OS sandbox.

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| V10 | Original failure function overwrote Complete and bridge-error/disconnect arm lacked terminal guard. | PR116 xhigh report | Verified | Original failure function overwrote Complete and bridge-error/disconnect arm lacked terminal guard. | Modify | Ignore late BridgeDisconnected/BridgeError/AgentConfigError once terminal exists. Preserve in-turn errors and explicit cleanup-failure override; regression covers late disconnect and late command error. | Implemented; current Linux regression and quality proof is recorded below. |
| S1 | Base never sends contentCollection setting and ignores its streamed option; pinned captures report enabled. Exact standard operation established by scout and new core-command owner. | PR116 xhigh report | Verified | Base never sends contentCollection setting and ignores its streamed option; pinned captures report enabled. Exact standard operation established by scout and new core-command owner. | Modify | Add standard SetConfigOption and require response-bound disabled confirmation, not just an unsolicited configuration update; later drift fails closed. Strictly decode returned options rather than trusting requested values or tolerant SDK defaults. | Implemented; current Linux regression and quality proof is recorded below. |
| S3 | Workspace feature unification masks non-KAS errors, including ungated timeout import; add isolated non-KAS coverage. | PR116 xhigh report | Verified | ci.yml default-features job selects --workspace, while core's Cargo.toml defines kas as default-off. spawn_isolation.rs timeout import is unconditional although all its unqualified uses are KAS-gated. Supplied report identifies workbench's kas dependency and resulting unused-import failure. | Accept | Cfg-gate timeout import. Add cargo clippy -p cyril-core --no-default-features --all-targets -- -D warnings and cargo nextest run -p cyril-core --no-default-features to existing default-features job; preserve workspace checks and real workbench dependency. | No new job needs wiring; existing ci-success already requires default-features. Verified by the startup/core checkpoint and current Linux gates. |
| V12 | Caller-runtime destruction could delete evidence before core completion; unanswered initialize also ignored last-client loss. | PR116 xhigh report and public bridge probe | Verified | Baseline public probe waited 6.9947 seconds for the peer's natural exit; removing lifetime observation makes the permanent fence time out. | Modify | EvidenceCleanup retains the tree through core completion even after executor destruction. A closure-only client-lifetime channel cancels unanswered initialize through existing core teardown; no second runtime/process owner. | Fixed probe completes in 2.0022 seconds before natural exit. Retained clone, dropped executor and lost-completion regressions pass. |
| V11 | Reported descendant-reaping race/native-Windows failure was not executed here; reviewer cannot strengthen core completion into an independently proven Windows descendant guarantee. | PR116 xhigh report | Unverified | Reported descendant-reaping race/native-Windows failure was not executed here; reviewer cannot strengthen core completion into an independently proven Windows descendant guarantee. | Reject (tracked at cyril-jlw9) | No copied process-group teardown or stronger empirical claim. Guard waits existing core completion. Main/core owns process lifecycle; native Windows remains an explicit uncertainty. | Verified open cyril-jlw9 owns Windows wrapper-grandchild/hard-consumer-death reaping; native reviewer execution is also gated by cyril-6y1s. Existing completion is not a Windows process-tree proof. |
| V15 | Windows canonical paths fail native glob validation; use compatible canonicalization. | PR116 xhigh report | Verified | runtime.rs canonicalizes to Windows extended syntax then rejects its question mark; std and dunce API contracts establish the representation mismatch. | Modify | Use dunce compatible canonicalization without removing semantically necessary prefixes; retain canonicalization and fail closed on unsupported policy paths; add Windows constructor coverage. | Native Windows execution remains qualified by cyril-6y1s; CI must compile/run platform tests. |
| S4 | ToolCallStatus exposes Failed but no reliable typed native-policy denial discriminator; ACP permission requests are a distinct channel. | PR116 xhigh report | Verified | ToolCallStatus exposes Failed but no reliable typed native-policy denial discriminator; ACP permission requests are a distinct channel. | Modify | Record bounded tool_failure_observed boolean separately. Do not inspect English payloads, titles, paths, or retain an unbounded failed-ID set. Document denied_permissions as delivered ACP cancellations. | Implemented; current Linux regression and quality proof is recorded below. |
| V14 | BridgeDisconnected reason, BridgeError message/operation, and AgentConfigError error were discarded. | PR116 xhigh report | Verified | BridgeDisconnected reason, BridgeError message/operation, and AgentConfigError error were discarded. | Modify | Distinct failure categories plus bounded private untrusted diagnostic and fixed operation projection. No arbitrary error payload logging. | Implemented; current Linux regression and quality proof is recorded below. |
| V13 | spawn_bridge error was discarded by map_err to a unit launch error. | PR116 xhigh report | Verified | spawn_bridge error was discarded by map_err to a unit launch error. | Modify | Retain bounded original Display diagnostic inside private ReviewDiagnostic; automatic Debug/Display remains redacted. No raw source-chain log. | Implemented; current Linux regression and quality proof is recorded below. |
| V4 | Inherited descendant pipe writers bypass the startup deadline; include EOFs in that deadline. | PR116 xhigh report | Refuted | Required base version.rs wraps child.wait and both readers in one VERSION_PROBE_TIMEOUT; drops ProcessGroupGuard before bounded error-path reap. Existing version_deadline_includes_exited_wrappers_inherited_pipes covers the public launch boundary. | Reject | Already fixed in required base; preserve shared three-second deadline and one-second reap. |  |
| V1 | Waiting before draining can deadlock verbose version probes; drain concurrently. | PR116 xhigh report | Refuted | Required base version.rs already uses tokio::try_join!(child.wait(), read_limited(stdout), read_limited(stderr)); readers keep draining to sink after their capture limits. Existing verbose_version_probe_drains_both_pipes_past_capture_limit exercises both streams above 1 MiB. | Reject | No additional concurrency remedy: already fixed in 1136dabd; preserve while fixing V5. |  |
| S5 | Original ?tool logs raw input/output/content and title. | PR116 xhigh report | Verified | Original ?tool logs raw input/output/content and title. | Modify | Log only typed tool kind/status, omitting even arbitrary tool IDs/titles. Added captured-log regression. | Implemented; current Linux regression and quality proof is recorded below. |
| V9 | Windows ignores auth_data_home and core callback uses ambient storage; bind or document the actual native owner. | PR116 xhigh report | Verified | Wrapper pins --auth-method cli; original Linux captures contain zero host-auth frames. Native Windows uses FOLDERID_LocalAppData, not XDG_DATA_HOME. cyril-tpwn tracks ambient core discovery/store resolution. | Modify | Validate Windows auth_data_home against the OS known folder rather than pretend XDG_DATA_HOME redirects it; document launcher-owned auth and the core fallback limitation. | Do not copy credentials, change native login ownership, or claim Windows live acceptance. Ambient Free/callback discovery remains tracked at cyril-tpwn. |
| V7 | Free+Replace runs host-environment discovery; reject or parameterize discovery. | PR116 xhigh report | Refuted | Base bridge.rs resolve_spawn_command rejects KAS Free+Replace before resolve_kas_command. Existing free_replacement_refuses_before_inherited_discovery has an inherited discovery positive control. | Reject | Required base already implements rejection; preserve it. |  |
| V8 | Replace+Host loads host hook registry and executes inherited-authority hooks; prevent the combination. | PR116 xhigh report | Verified | domain_mediator/mod.rs:232-240 loads HookRegistry using host home_dir for HooksAdapter::Inbound; engine.rs:294-298 maps Host to Inbound. Base resolve_spawn_command only rejects Free+Replace, leaving Wrapper+Replace+Host accepted. | Modify | bridge.rs extends existing startup rejection to KAS Replace+Host before probe/spawn/DomainMediator::new. Isolated test configs explicitly use Off. New replacement_host_hooks_refuse_before_probe_or_agent_start fences launch rejection. Existing nested Free test additionally proves Inherit+Host still advertises enabled hooks under private HOME. | No registry/environment threading abstraction. Verified by the startup/core checkpoint and current Linux gates. |
| S2 | Launcher cloud sync may restore org MCP/hooks before readiness; probe entitlement or bypass launcher. | PR116 xhigh report | Unverified | Pinned capture reports cloud-config result notEnabled; no entitled-account sync/injection was exercised. Environment-name presence proves only a probe, not sync. | Reject (tracked at cyril-6y1s) | N/A — no demonstrated bypass. Post-sync rejection cannot undo pre-prompt MCP/hook effects, and changing spawn/auth paths requires new native proof. | Configured native isolation acceptance must include the entitled-account case; related cyril-0b8t documents cloud-config behavior, not an isolation fix. |
| V22 | Original model boolean could latch from wrong-mode config and survive mode-only switch. | PR116 xhigh report | Verified | Original model boolean could latch from wrong-mode config and survive mode-only switch. | Modify | Process mode before model regardless of option order; clear model while mode unconfirmed; accept model only under confirmed reviewer mode. Wrong ModeChanged/AgentSwitched invalidates readiness. Preserve post-start drift across continuations. | Implemented; current Linux regression and quality proof is recorded below. |
| V16 | KAS host callbacks are unscoped; disable adapters or scope responders. | PR116 xhigh report | Unverified | Adapters are installed, but captured pinned KAS operations are denied before callbacks; no policy-bypassing callback is demonstrated. Staging creates no evidence symlinks. | Reject (tracked at cyril-6y1s) | N/A — do not add a second policy/runtime boundary for an unverified bypass. | Native policy is the selected authority. Replace is not an OS filesystem sandbox; exact-version qualification and native acceptance remain load-bearing. |
| C16 | Required evidence is untracked; archive sources/captures and ignore generated outputs. | PR116 xhigh report | Verified | 1136dabd tracks only route/shape; restored 49 missing files from the original evidence worktree. Count-only scan found no original home prefix, JWT, provider key, private key or credential-bearing URL. | Accept | Publish the sanitized historical evidence in a linked, independently reviewable archive PR; retain provenance and narrow .gitignore. | Original captures are historical proof, not rerun results; build outputs and nested Cargo.lock are excluded. |
| V5 | Timeout discards diagnostics already emitted; include captured stderr in failure. | PR116 xhigh report | Verified | Base read_limited owns its Vec; timeout cancels the reader and destroys partial captured bytes, leaving only generic deadline text. | Modify | version.rs moves bounded capture Vecs outside cancellable readers and borrows them; timeout includes captured nonempty stderr without another pipe read. Existing hanging_version_probe_fails_before_workbench_deadline now emits and requires probe-auth-diagnostic. | No unbounded post-kill EOF wait; capture remains capped at 1 MiB. Verified by the startup/core checkpoint and current Linux gates. |
| V21 | Both nested harnesses can return success after matching zero tests; attest inner execution. | PR116 xhigh report | Verified | Base spawn_isolation.rs self-reexec branches only check status.success(); their --exact literals are independent from test identifiers. Neither parent originally observed proof the body ran. | Modify | Each nested body writes its own completion file only after all assertions; its parent requires that exact completion payload in a fresh TempDir. First test recursion uses an explicit completion-path variable rather than preexisting canary presence. | Externally observed sentinels, not libtest English parsing. Verified by the startup/core checkpoint and current Linux gates. |
| V19 | Replace omitting PATH changes executable lookup without documentation; clarify the contract. | PR116 xhigh report | Verified | spawn_environment.rs apply calls env_clear().envs(values), with no fallback. Existing public type documentation omitted lookup behavior. | Accept | Document platform Command lookup, recommend absolute executable paths or explicit PATH, and clarify Cyril never restores omitted parent PATH. SpawnConfig.environment docs link this contract and state hook restriction. | Documentation-only; no parent-PATH fallback or new validation policy. |
| V20 | A zero evidence-byte limit admits empty documents; reject zero. | PR116 xhigh report | Verified | Validation compares actual bytes against the cap; approved design explicitly permits empty document text. Document count, instruction, labels and output remain bounded. | Reject | N/A — zero bytes is a valid strict cap, not a missing value or safety hole. | Rejecting it would arbitrarily remove an explicitly approved input shape. |
| C13 | The workbench lacks a numbered roadmap owner. | PR116 xhigh report | Verified | Base roadmap has no inspection-workbench entry; Phase 5+ is the existing composable-platform owner. | Accept | Add the Phase 5+ reviewer/workbench entry with PR and retained-evidence links. | Do not mark the desktop, publication, Findings or native Windows release gates complete. |
| V17 | Concurrent native and WSL bridges share process-global path mode. | PR116 xhigh report | Unverified | Mechanism is pre-existing; neither the TUI nor a current desktop consumer co-hosts native reviewer and WSL bridge. The approved desktop target is native-only. | Reject | N/A — mixed native/WSL hosting is outside this reviewer operation. | Permanent non-goal here; no claim that the global path mode is safe for a hypothetical mixed host. |
| C14 | Public serialization variant exposed serde_json::Error and lacked document context. | PR116 xhigh report | Verified | Public serialization variant exposed serde_json::Error and lacked document context. | Modify | Domain Serialization { document, diagnostic } mappings cover manifest, profile and policy; diagnostics are private, bounded and automatically redacted. | Linux compile, error and redaction checks pass. |
| C15 | Lost result channel previously fabricated String::new() despite unknown/lost accumulated output. | PR116 xhigh report | Verified | Lost result channel previously fabricated String::new() despite unknown/lost accumulated output. | Modify | Incomplete partial_text is Option<String>; TaskLost returns None. Owned callsites and tests migrated; the smoke source was migrated and then archived outside Cargo examples. | Lost output is distinct from an observed empty output; regression passes. |
| C1 | Duplicated feature-dependent guards might drift; introduce SpawnConfig::validate. | PR116 xhigh report | Verified | bridge.rs has two cfg-specific resolve_spawn_command functions with required-version rejection. In non-KAS builds engine_for rejects Kas before command resolution; no inconsistent accepted configuration is demonstrated. | Reject | N/A — duplication exists but no present defect. A new validation method would grow a protected module and add a seam without demonstrated need; isolated CI addresses untested feature paths. |  |
| C2 | Move the exact CLI pin into caller configuration and accept semver suffixes. | PR116 xhigh report | Verified | runtime.rs fixes CLI_VERSION=2.21.1; approved native policy proof and design explicitly qualify that exact token. | Reject | N/A — keep the exact deployment qualification. | A caller-configurable pin or suffix normalization would let an unqualified binary bypass the safety gate. Version upgrades require fresh proof, not a convenience parameter. |
| C3 | Two Kiro-home resolvers may drift; parameterize shared kiro_agent_config functions and add inherited wrappers. | PR116 xhigh report | Verified | settings.rs:50-59 and kiro_agent_config.rs:86-97 share nonempty KIRO_HOME then HOME/USERPROFILE precedence. Current settings resolver intentionally reads selected environment; no current divergent result was demonstrated. | Reject | N/A — speculative consolidation would broaden caller/cfg changes and introduce inherited wrappers. Preserve current run-local semantics without expanding scope. |  |
| C4 | Probe duplicates process-group teardown and leaks on early return; consolidate lifecycle ownership. | PR116 xhigh report | Refuted | Required base version.rs already creates transport::ProcessGroupGuard from child.id and explicitly drops it before bounded reap. transport.rs constructor is pub(crate), and its Drop treats ESRCH as success; child also has kill_on_drop(true). | Reject | Already resolved with existing guard reuse. No need to promote a larger ProcessTree abstraction or migrate unrelated terminal ownership. |  |
| C5 | Remove the completed throwaway smoke example or export its environment list. | PR116 xhigh report | Verified | The example header and approved plan require removal after native capture; historical native evidence exists and is now preserved. | Accept | Remove the workspace example after rerunning current native acceptance; historical replay retains its explicit base-revision recipe. | Do not widen the public reviewer API solely for a throwaway driver. |
| C6 | Manifest already streams through BufWriter, flushes before spawn, and preparation failure prevents spawn. Proposed to_vec adds up to roughly 4 MiB allocation without fixing demonstrated behavior. | PR116 xhigh report | Not-applicable | Manifest already streams through BufWriter, flushes before spawn, and preparation failure prevents spawn. Proposed to_vec adds up to roughly 4 MiB allocation without fixing demonstrated behavior. | Reject | Keep streaming manifest. | Keep streaming manifest. |
| C7 | Per-file sync_all calls serve transient same-host trees, not durable retained records. | PR116 xhigh report | Verified | Per-file sync_all calls serve transient same-host trees, not durable retained records. | Accept | Remove sync_all calls; retain write_all and buffered manifest flush before spawn. No latency improvement claimed without execution. | Implemented; current Linux regression and quality proof is recorded below. |
| C8 | Duplicate cancellation decisions and unreachable terminal-None outcome arm existed. | PR116 xhigh report | Verified | Duplicate cancellation decisions and unreachable terminal-None outcome arm existed. | Modify | Consolidated apply_cancellation, removed redundant channel-close assignment and None outcome arm. Core completion establishes terminal before cleanup, preserving failure precedence. | Implemented; current Linux regression and quality proof is recorded below. |
| C9 | Suggested large teardown/readiness enums address hypothetical future combinations, not a demonstrated reachable defect. | PR116 xhigh report | Not-applicable | Suggested large teardown/readiness enums address hypothetical future combinations, not a demonstrated reachable defect. | Reject | Keep existing concrete state machine; no broad enum rewrite. | Keep existing concrete state machine; no broad enum rewrite. |
| C10 | Source values were discarded; source_observer.rs explicitly tolerates Closed in try_emit and finish. | PR116 xhigh report | Verified | Source values were discarded; source_observer.rs explicitly tolerates Closed in try_emit and finish. | Modify | Drop source receiver immediately at bridge split; remove source drain and open flag. Do not retain receiver as _sources. | Implemented; current Linux regression and quality proof is recorded below. |
| C11 | SessionController does not supply catalog membership and required confirmation ordering; replacement would require unrelated core changes. | PR116 xhigh report | Not-applicable | SessionController does not supply catalog membership and required confirmation ordering; replacement would require unrelated core changes. | Reject | Keep reviewer-specific readiness policy local. | Keep reviewer-specific readiness policy local. |
| C12 | Use SpawnEnvironment in ReviewerConfig to reuse Debug redaction. | PR116 xhigh report | Not-applicable | N/A — a type-design preference; the existing explicit map cannot express Inherit and its manual Debug redacts values. | Reject | N/A — preserve the narrower caller contract. | The proposed enum introduces an invalid configuration solely to remove a short redaction implementation. |
| C17a | Three readiness failures shared one sequential test and two-second deadline. | PR116 xhigh report | Verified | Three readiness failures shared one sequential test and two-second deadline. | Accept | Split catalog-missing, wrong-mode, and missing-configuration tests; shared fixture startup deadline is one second. Load margin remains for Main to execute. | Implemented; current Linux regression and quality proof is recorded below. |
| C17b | Fixed five-second stall sampler delays tests with short thresholds; derive cadence from threshold. | PR116 xhigh report | Verified | domain_mediator/mod.rs:307-308 explicitly uses a five-second interval, independently from threshold supplied to liveness checks. Supplied review records this distinction and test timing. | Reject | N/A — changing notification sampling semantics solely to shorten tests is not an established bug fix and would modify a protected body outside ownership. Preserve existing sampler semantics. |  |
| V2 | Inherited probe stdin may stop background process-group children; restore null stdin. | PR116 xhigh report | Refuted | Required base version.rs command explicitly uses stdin(Stdio::null()). | Reject | Already resolved; no stdin change needed. |  |
| V3 | try_wait error returns without kill/reap and can leak a child. | PR116 xhigh report | Refuted | Required base has no synchronous try_wait poll; async wait errors enter bounded kill/reap, and group Drop plus kill_on_drop cover ownership exits. | Reject | Stale lifecycle claim; retain base implementation. |  |
| V6 | Synchronous poll sleep pins the bridge runtime; make probe async. | PR116 xhigh report | Refuted | Required base version.rs uses tokio process, async reads, try_join, and timeout; no thread sleep remains in probe. | Reject | Async conversion already in required base. |  |
| V18 | Changing watch send method does not remove watch borrow locking; current owned consumers use temporary borrows, and nonreading observer is already fenced. | PR116 xhigh report | Refuted | Changing watch send method does not remove watch borrow locking; current owned consumers use temporary borrows, and nonreading observer is already fenced. | Reject | Document that subscribers must not hold watch borrows across awaits; retain watch projection. | Document that subscribers must not hold watch borrows across awaits; retain watch projection. |
| C17c | event() polls small local fixture journals; replacing polling with filesystem notification machinery has no demonstrated benefit. | PR116 xhigh report | Refuted | event() polls small local fixture journals; replacing polling with filesystem notification machinery has no demonstrated benefit. | Reject | Keep existing polling helper. | Keep existing polling helper. |
| C17d | Remove self-reexec for efficiency or simplify it using a snapshot of parent variables. | PR116 xhigh report | Refuted | The report itself rejects the efficiency claim and presents removal as optional simplification. Reexec supplies an explicit controlled canary without mutating the harness environment. | Reject | Keep deterministic seeded-parent isolation and fix its real vacuity defect under V21; no environment-snapshot redesign. |  |
| Rextglob | Extra +/@ glob exclusions are required. | PR116 xhigh report | Refuted | Report inspected KAS noext:true matcher; retained pinned native policy controls verify the selected scope recipe. | Reject | N/A — preserve the native matcher-compatible metacharacter boundary. | Windows prefix repair must not widen the authorized subtree. |
| Rcapabilities | Native mcp/shell capability names or read_file tool id are invalid. | PR116 xhigh report | Refuted | Pinned native inventory, successful permissive write/shell controls and failed restricted operations exercise these exact native identifiers. | Reject | N/A — retain the live-proven native policy vocabulary. | Model refusal alone is not the cited proof. |
| Rcancel | Permission responder cancellation and authoritative prompt completion are distinct paths; existing peer scenario completes after three cancelled permission responses. | PR116 xhigh report | Refuted | Permission responder cancellation and authoritative prompt completion are distinct paths; existing peer scenario completes after three cancelled permission responses. | Reject | No synthetic turn termination. | No synthetic turn termination. |
| Rpop | Live drive's terminal/readiness guards imply an established session and retained sender before dispatch; sender is taken only during terminal shutdown. | PR116 xhigh report | Refuted | Live drive's terminal/readiness guards imply an established session and retained sender before dispatch; sender is taken only during terminal shutdown. | Reject | No speculative dispatch refactor. | No speculative dispatch refactor. |
| Rpresent | A missing present_as selects the wrong client identity. | PR116 xhigh report | Refuted | Existing engine default resolves None to KiroCli; original native initialize and successful reviewer captures exercise that path. | Reject | N/A — retain the existing presentation default. | No second reviewer-specific identity setting is needed. |
| Rshape | The shape walker skips the production sibling after a test item. | PR116 xhigh report | Refuted | walk resets skip when consuming the attributed item, continues without descending into it, then traverses the next sibling. | Reject | N/A — preserve traversal; run its existing mutation controls. | No test-skipping workaround or widened protected-parent exemption. |
| Rdeadline | Current input explicitly supports continuations, so review report's single-prompt rationale is stale. Immediate continuation dispatch normally avoids rearmed deadline, but readiness policy should apply only before inspection starts. | PR116 xhigh report | Refuted | Input supports continuations, but TurnCompleted dispatches the next prompt in the same synchronous notification handler; the readiness timer cannot observe an intermediate prompt_sent=false state. Terminal outcomes also disable it. | Reject | N/A — the report's single-prompt rationale is stale, but the alleged timer failure is not reachable. | A sticky inspection-started marker makes the existing initial-readiness policy explicit; do not claim a reproduced deadline regression. |
| Fgroup | Typed outer decoding still silently discarded malformed grouped choices. | Final correction reviewer | Verified | Pinned schema SessionConfigSelectGroup.options uses DefaultOnError<VecSkipError>; the new grouped-choice fixture reproduced a partial success acknowledgement. | Modify | Decode once from borrowed JSON and reject non-array or shortened decoded groups before acknowledgement. Keep valid grouped catalogs and optional presentation metadata support. | Independent core regression is red before the loss check and green after it; no duplicate catalog allocation or schema clone. |

## Verification record

Startup slice 3 PASS: `review-checkpoint-startup.json` records current workspace/core-only tests, both clippy selections, formatting, bounded probe observations and the unchanged shape budget. `review-startup-controls.txt` retains the three deliberately red mutations; restored fences are green. V1/V4/V7 preserve fixes already present in the assessment base; V5/V8/V19/V21/S3 are now implemented and checked.

Current Linux proof: 1,923 workspace tests passed; after test-helper error propagation, all 46 workbench tests, workspace clippy, 739 standalone non-KAS core tests, non-KAS core clippy, formatting and the complete shape oracle with all three mutations pass. PR #118 independently verifies the core increment without a workbench member/dependency: 1,604 workspace tests, 1,012 KAS core tests, both clippy selections, formatting and slice shape controls pass.

`review-runtime-controls.txt`, `review-core-controls.txt` and `review-grouped-control.txt` retain deliberately red privacy, terminal, tolerant-decoder, grouped-choice and last-client controls. Their corrected behavior is exercised by the green suites. A fingerprint fixture now retains its client through rejection rather than accidentally selecting owner cancellation; its fingerprint/error assertions are unchanged.

`review-native/` retains current native CLI 2.21.1 consumer captures separately from the original historical captures. Its archived Rust drivers are not Cargo examples. To replay, temporarily copy `review-smoke-source.rs` to `crates/cyril-workbench/examples/review_smoke.rs`, build that example, run `python3 .cyril-jlxx/review-native/native-acceptance.py`, then remove the temporary example. The result records actual synthetic-file/process/environment observations; model prose is not the authority.

Final native replay at 07:22 UTC was blocked before initialize: both isolated launchers and ordinary `kiro-cli whoami` reported not logged in. `review-native/prior-success.json` and `prior-{A,B}.*` retain the successful 06:35 run before the grouped-choice-only correction; `auth-blocked-*` retains the later failure. No final-revision native success is inferred from the earlier run. A targeted source audit found native auth reads use SQLite READ_ONLY and located test mutations use temporary fixture stores; no destructive real-store test path was found. Native renewal/account-state cause is unestablished; user sign-in is required to repeat the final live gate.

Windows construction is checked by the Windows CI leg and `oracles/windows-construction.py`: each fixed case must pass, each compiling mutation must fail, and restored source must pass. That gate does not establish native KAS authentication, descendant reaping or entitled-account cloud isolation; cyril-6y1s, cyril-jlw9 and cyril-tpwn retain their explicitly described obligations.

## Delayed advisory reconciliation

These records concern the published `a1ac53c7` reviewer / `62437d73` core state, not the original 54-record report.

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| Acommand | Command inventory omits SetConfigOption and still pins 20 entries. | Delayed advisory | Refuted | current_runtime_contract/saturation.rs:28 includes the command; the FIFO test derives its expected length. | Reject | N/A — current fixture already migrated. | No count-only assertion restored. |
| APython | Configuration fixture assumes /usr/bin/python3; select runnable host Python instead. | Delayed advisory | Verified | Baseline62437d73 used a fixed-path shebang and restricted PATH. Its protocol test passed but bypassed the selected host Python; the selection control was red. | Modify | Core7477df72 resolves sys.executable before environment replacement and launches explicit -I/script argv; the corrected control and exchanges pass. | This is a verified host-layout assumption, not a reproduced missing-interpreter or macOS runtime failure. |
| ATracked | Three flagged files may be absent from published commits. | Delayed advisory | Refuted | git ls-tree at both published heads includes spawn_environment.rs, spawn_isolation.rs and session_configuration.rs with identical blobs. | Reject | N/A — all files are committed. | Immutable trees, not unstaged diff output, decide this claim. |
| AGates | Final local proof lacks delivered job completion. | Delayed advisory | Refuted | artifact 516 contains completed test/clippy/shape results; CI 34098257297 completed all 13 jobs successfully. | Reject | N/A — delivered results already establish controlled proof. | Native authentication failure is not part of that PASS. |
| ANative | Auth-blocked replay must not be counted as native success. | Delayed advisory | Verified | Current checkpoint is CONTROLLED_PASS_NATIVE_REPLAY_BLOCKED and retains prior-success versus auth-blocked captures separately. | Reject | N/A — preserve the existing qualification. | No runtime workaround, credential access or weakened diagnostic boundary. |
| AFd | Independent descriptor control does not attribute the exact original fixture failure. | Delayed advisory | Verified | Control demonstrates inherited writable descriptors; no original failing PID/fd trace was captured. Source contains overlapping executable rewrites and process creation without the guard. | Modify | Qualify the checkpoint as source-inferred attribution; retain test-only ownership control and preconstructed tokio::join! pair. | Observed ETXTBSY and successful control are facts; exact historical attribution is not. |
| APlan | Plan has projections but no measured post-partition revision pairs. | Delayed advisory | Verified | Published tree differences are 2102 core and 3705 reviewer lines; current plan paragraph remains projected arithmetic. | Modify | Record immutable revision-pair measurements separately from estimates; preserve margin and ownership. | Fixture guard commit 9fafca05 is an ancestor of core62437d73. |
| AWorktree | Failed primary-checkout push requires safe worktree publication. | Delayed advisory | Verified | Specialized push rejected main metadata; explicit non-force push from the prepared pr-116 worktree published a1ac53c7. | Modify | Use explicit prepared-worktree cwd for publication; do not reset/re-checkout the already validated branch. | Primary source checkout remains untouched. |
| ADraft | No-merge prose does not establish draft status. | Delayed advisory | Verified | GitHub conversion succeeded; API reread reports isDraft=true for PR116 at a1ac53c7. | Accept | Explicitly mark PR116 draft while final native acceptance is blocked. | No merge or readiness claim. |
| ARecords | Earlier accepted repairs lack same-surface compact provenance records. | Delayed advisory | Verified | The ledger originally linked aggregate checkpoints but only APython had a compact record. | Modify | Add the original atomic-repair records below with ownership, paths, exact commands/results and retained-evidence applicability. | Evidence reconciliation only; no new behavior or retroactively invented result. |
| AOracle | Artifact558 predates the control's shlex.quote edit. | Delayed advisory | Verified | Artifact558 remains valid test/clippy proof but is not the current oracle revision. | Modify | Retain a fresh post-shlex run in python-selection-postquote.json with source hashes, exact command and actual results; pin CI checkout/head tree equivalence. | No production, test or oracle code changed in this reconciliation. |

### APython bounded proof repair

Ownership: C1/C2/C7, existing slice 4 generic configuration prerequisite. Only `tests/session_configuration.rs` launch setup changes; its single caller is the existing configuration exchange test (LSP missed it; scoped search located it). Reuse structured AgentCommand argv and Python's sys.executable; no core resolver dependency exists. Resolve before replacing HOME/PATH so a host version-manager shim cannot depend on the peer's private HOME.

Proof: baseline protocol execution passed while host-selection failed; this was not merely a --no-run compile. The corrected selection control and both feature selections pass. Artifact558 records test/clippy proof, not the later quoting revision; `review-native/python-selection-postquote.json` pins the fresh post-shlex execution and source hashes. `review-checkpoint-core-runtime.json` records all nine bounded gates plus CI34102117724: all 13 jobs passed on a8577505. CI's temporary-main checkout d5afbef and the published head have identical trees; the stacked base was restored. Native acceptance remains auth-blocked.

## Original compact repair records

These records reconcile already-executed repairs; they do not claim new executions. Explicit paths are repository-relative; core source paths resolve under `crates/cyril-core/src/`, workbench source paths under `crates/cyril-workbench/src/`, and short proof/oracle paths under `.cyril-jlxx/`. Unchanged fields inherit `plan.md` slices 3–5 and claims C1–C7. Common proof: artifact516 records `cargo test --workspace` (1,923 passes), `cargo test -p cyril-core --no-default-features` (739 passes), workspace/standalone-core all-target clippy with `-D warnings`, formatting, and `python3 .cyril-jlxx/oracles/shape.py --complete --mutation-check` (PASS). CI34102117724 passed all 13 jobs after APython's test-only launcher repair. These record-only amendments leave checked code and inputs unchanged. Native acceptance remains blocked, not a retained PASS.

### R-version — V5
- Ownership: C2/C6, slice3; startup commit8f028ebd, inherited by core a59acd43.
- Root/paths: retain bounded version-probe buffers outside cancellable readers in `crates/cyril-core/src/protocol/kas/version.rs`; strengthen `crates/cyril-core/tests/spawn_isolation.rs`. No second pipe drain or timeout owner.
- Commands/results: `cargo test -p cyril-core --features kas --test spawn_isolation` observes pre-timeout diagnostic bytes and bounded completion; discarding captured timeout stderr is red in `review-startup-controls.txt`, restored suite green.
- Evidence disposition: pre-fix diagnostic loss is superseded. `review-checkpoint-startup.json` and final assembled proof remain applicable; capture limit and production path are unchanged by later fixture/docs repairs.

### R-hooks — V8, V19
- Ownership: C2, slice3; startup8f028ebd/core a59acd43.
- Root/paths: reject KAS Replace+Host before discovery/probe in `crates/cyril-core/src/protocol/bridge.rs`; document replacement lookup in `crates/cyril-core/src/types/spawn_environment.rs`; `spawn_isolation.rs` holds private-HOME controls.
- Commands/results: `cargo test -p cyril-core --features kas --test spawn_isolation` proves rejection before marker creation while Inherit+Host remains functional. Removing rejection is red in `review-startup-controls.txt`; restored cases pass.
- Evidence disposition: hypothetical parameter-threading fix rejected; existing validation/teardown reused. Final Linux and CI proof retained; no Windows/native-auth enforcement inference.

### R-nested — V21
- Ownership: C2, slice3; startup8f028ebd/core a59acd43.
- Root/paths: `crates/cyril-core/tests/spawn_isolation.rs` writes completion sentinels only after nested assertions, then requires them in the parent.
- Commands/results: `cargo test -p cyril-core --features kas --test spawn_isolation`; deliberately stale --exact filters fail the external sentinel check (`review-startup-controls.txt`); restored nested bodies and parents pass.
- Evidence disposition: status.success alone is no longer proof. Later fixture serialization preserves these sentinels; artifact516 and both platform CI runs execute them.

### R-feature-selection — S3
- Ownership: C7, slice3; startup8f028ebd/core a59acd43.
- Root/paths: `.github/workflows/ci.yml` selects standalone core without default features, rather than relying on workspace feature unification; cfg-qualified imports remain in `crates/cyril-core/tests/spawn_isolation.rs`.
- Commands/results: `cargo test -p cyril-core --no-default-features` and `cargo clippy -p cyril-core --no-default-features --all-targets -- -D warnings` pass; the CI Default Features job's `cargo nextest run -p cyril-core --no-default-features` passes in CI34102117724.
- Evidence disposition: workspace-only inference superseded by actual isolated selection. Final 739-pass local result and CI retained; no lint suppression or dependency workaround.

### R-readiness — S1, V22
- Ownership: C1/C2, slice4; core a59acd43 plus reviewer b19b75c6.
- Root/paths: standard command/response in core `types/event.rs` and `protocol/domain_mediator/commands/{mod,session}.rs`; existing core `session.rs`, UI `state.rs`, bridge example and command inventory migrate atomically. Workbench `reviewer.rs` requires response-bound disabled collection and post-profile model confirmation; `tests/reviewer.rs` and its peer exercise ordering.
- Commands/results: `cargo test -p cyril-workbench` passes the no-early-prompt, rejected/coerced/missing acknowledgement, drift and profile-order cases; `cargo test -p cyril-core --no-default-features --test session_configuration` preserves response versus unsolicited event identity. Removing privacy readiness is red in `review-runtime-controls.txt`, restored suite green.
- Evidence disposition: requested-value/unsolicited-update trust is superseded. Artifact516 and CI retain controlled proof; the earlier successful native run is historical and the final sign-in-gated replay is not upgraded.

### R-decoder — Fgroup
- Ownership: C1/C2, slice4 generic prerequisite; core a59acd43.
- Root/paths: `crates/cyril-core/src/protocol/domain_mediator/commands/session.rs` decodes borrowed JSON once and rejects malformed/lost required grouped choices; `crates/cyril-core/tests/session_configuration.rs` supplies valid and malformed catalogs.
- Commands/results: `cargo test -p cyril-core --no-default-features --test session_configuration` preserves server-selected grouped values and rejects malformed flat/grouped data. Tolerant outer/grouped controls are red in `review-core-controls.txt` and `review-grouped-control.txt`; restored exchange passes.
- Evidence disposition: partial acknowledgement is superseded. APython changes only launch setup, not catalog inputs/assertions; refreshed exchanges, the macOS CI case and retained decoder controls establish applicability.

### R-lifetime — V12, C8, C10
- Ownership: C5/C6/C7, slice4; core a59acd43 plus reviewer b19b75c6.
- Root/paths: core `protocol/bridge.rs` handles and `protocol/domain_mediator/mod.rs` observe final-client loss during initialize through existing teardown. Workbench `reviewer.rs` and `reviewer/evidence.rs` retain evidence across executor loss, consolidate cancellation and drop the unused source receiver.
- Commands/results: `cargo test -p cyril-core --features kas --test spawn_isolation` preserves a retained clone and completes before peer natural exit after final drop; `cargo test -p cyril-workbench` covers dropped/unpolled executor and lost completion. Removing client-loss observation is red (`review-core-controls.txt`); corrected public probe completes at 2.0022s versus baseline natural exit at 6.9947s (`review-native/initialize-drop-results.json`).
- Evidence disposition: waiting for completion alone is no longer timely-shutdown proof. Existing process ownership is retained, not copied. Final controlled gates apply; unknown/native Windows descendants remain qualified.

### R-outcome — V10, S4, C15
- Ownership: C5/C6, slice4; reviewer b19b75c6.
- Root/paths: workbench `reviewer.rs` and `reviewer/types.rs` preserve authoritative completion over late errors, distinguish observed tool failure from permission verdict, and represent unavailable partial output as None.
- Commands/results: `cargo test -p cyril-workbench` passes terminal precedence, in-turn errors, cleanup override, failed-tool observation and lost-output cases. Reintroducing late-error demotion is red in `review-runtime-controls.txt`; corrected cases pass.
- Evidence disposition: synthetic denial and fabricated empty output are not retained behavior. Public consumers/archived driver use the new result contract; artifact516 and CI remain applicable.

### R-private-diagnostics — V13, V14, C14, S5
- Ownership: C4/C6, slice4; reviewer b19b75c6.
- Root/paths: workbench `reviewer.rs`, `reviewer/{types,evidence,runtime}.rs` preserve bounded private synchronous/asynchronous/serialization diagnostics while redacting automatic formatting and logging only typed tool metadata.
- Commands/results: `cargo test -p cyril-workbench` passes UTF-8 truncation, explicit cause retrieval, Debug/Display redaction and captured-log canary checks; artifact516 and CI34102117724 include the concrete private-diagnostic and tool-observation cases.
- Evidence disposition: generic error erasure/raw logging superseded by typed private diagnostics. No raw exception, tool payload or credential inspection is introduced; no new mutation execution is claimed by this record-only amendment.

### R-transient-io — C7
- Ownership: C3/C6, slice4; reviewer b19b75c6.
- Root/paths: `crates/cyril-workbench/src/reviewer/evidence.rs` removes transient fsync calls while retaining write/flush/error completion before spawning.
- Commands/results: `cargo test -p cyril-workbench` passes complete staging/read cases and staging-IO failure with no child; artifact516 and CI include these cases.
- Evidence disposition: no latency improvement or crash-durability requirement is invented. This remains private transient evidence, not a persistent store; native replay qualification is unchanged.

### R-readiness-fixtures — C17a
- Ownership: C1, slice4 technical proof; reviewer b19b75c6.
- Root/paths: `crates/cyril-workbench/tests/reviewer.rs` and its fixture separate absent catalog, absent configuration and wrong-mode cases under the shared one-second startup bound.
- Commands/results: `cargo test -p cyril-workbench` passes all 46 cases, including the split failures; CI34102117724 macOS logs show each isolated negative case passing rather than one combined assertion masking another.
- Evidence disposition: earlier combined timing proof is superseded by individual outcomes. No production deadline or successful-silence behavior changes; final local/CI evidence retained.

### R-windows-construction — V15, V9
- Ownership: C1/C2/C3, slice5; reviewer b19b75c6.
- Root/paths: workbench `reviewer/runtime.rs`, `reviewer/types.rs`, root/workbench Cargo manifests and lockfile, and `.github/workflows/ci.yml`; `.cyril-jlxx/oracles/windows-construction.py` checks compatible canonicalization and Known Folder auth-parent equality.
- Commands/results: `python .cyril-jlxx/oracles/windows-construction.py` in Windows CI requires fixed constructor cases green, raw-canonicalization and removed-auth-guard mutants red, restored cases green. CI34098257297's named step and all jobs in CI34102117724 pass.
- Evidence disposition: Linux source inference is superseded by native Windows constructor execution. This is not native KAS authentication, cloud isolation or descendant acceptance; those qualifications and the final sign-in blocker remain.

### R-executable-fixture-ownership — AFd
- Ownership: C2/C5, slices3/4 fixture proof; core9fafca05, qualified by7477df72.
- Root/paths: `crates/cyril-core/tests/spawn_isolation.rs` serializes executable creation/process lifetime between tests and preconstructs the intentionally concurrent pair before tokio::join!.
- Commands/results: original verbose fixture reports ETXTBSY (artifact509); `python3 .cyril-jlxx/review-native/executable-fd-probe.py` independently demonstrates inherited-writer ETXTBSY followed by successful execution on release. Corrected `cargo test -p cyril-core --features kas --test spawn_isolation` reports 12 passes including nested cases; full artifact516 and CI pass.
- Evidence disposition: exact historical PID/fd attribution remains inferred, not demonstrated. The fix is test-only and preserves intended launch concurrency; no production retry or process manager is added.

Nonbehavioral records C13/C16/V19 retain their roadmap/archive/contract evidence in the finding rows. C5 removes the Cargo smoke driver after capturing the final attempted native replay; source remains archived and all-target checks include its removal. They do not require invented runtime checkpoints. ARecords/AOracle add only provenance; current proof sources and all application/test code remain unchanged.
