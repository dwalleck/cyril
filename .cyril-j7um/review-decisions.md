# Review decisions: cyril-j7um advisor findings

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| F1 | `MemoryRuntimeHandle::changed()` silently maps a closed watch channel to `None`; match the closure, log it, and project an explicit terminal state. | system advisor | Verified | Direct code check found `self.status.changed().await.ok()?` at `crates/cyril/src/memory_runtime.rs`; the mutation made `closed_status_channel_transitions_once` fail before receiving a terminal view. | Accept | Review-fix Slice 5 adds one-shot closed-channel state, explicit Failed/Degraded projection, contextual warning, and a red/green regression fence. | Focused test and workspace clippy PASS locally; no behavior or architecture approval change required. |
| F2 | Windows named-pipe privacy/authentication was acceptance-critical but the existing test only checked a display label; exercise the real pipe ACL and invalid-auth path on Windows. | system advisor | Verified | Direct test inspection found `windows_endpoint_uses_current_user_protected_acl` asserted only that display text contained `pipe`; prior Windows CI proved compilation and same-user health but did not inspect the live handle DACL or send a wrong credential. | Modify | Review-fix Slice 5 queries the accepted server pipe with `GetSecurityInfo`, checks protected current-user-only access, and adds wrong-credential rejection followed by valid health. | Modified rather than recording FAIL because native Windows CI is available; Windows verification is required before completion. |
