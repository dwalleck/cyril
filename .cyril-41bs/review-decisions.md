# Review decisions: cyril-41bs decision slice

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| F1 | C14's route check allegedly required a marker absent from `route.md`; remove or replace that check. | `DecisionSliceReview` | Refuted | `route.md` T4 contains literal `**Deliverables:**`; every C14 run reported `route_deliverables_present: true`. The reviewer retracted the finding after reading the untruncated line. | Reject | N/A — the check was already grounded; it was strengthened to require both the required-artifacts section and T4 deliverable clause. | No behavior defect existed. |
| F2 | `design.md` still described C14 as PENDING after its checkpoint passed; update the lifecycle status. | `DecisionSliceReview` | Verified | Direct read found C14 and the summary still PENDING while `.cyril-41bs/checkpoints/C14.json` recorded PASS. | Accept | Review-fix slice changes C14 to PASS and removes it from the remaining-PENDING list. | Lifecycle-only design update; approved architecture and requester words are unchanged. Gate: C14 review-fix checkpoint. |
| F3 | File-wide PASS counting could let a failed P1–P10 premise hide behind PASS rows in the comparison table; parse exact premise rows. | `DecisionSliceReview` | Verified | Changing the first P1 verdict to FAIL while the comparison table stayed PASS reproduced the ambiguity; the revised row parser reported `P1: FAIL` and made C14 red. | Accept | Review-fix slice parses the first exact P1–P10 status row and requires every status to start with PASS. | Gate: P1 FAIL mutation red, restoration green, final C14 review-fix checkpoint. |
| F4 | Checking only two exact future production filenames cannot prove the spike contains no production migration; broaden the production fence. | `DecisionSliceReview` | Verified | Source audit showed only `sdk_runtime.rs` and `domain_mediator.rs` were checked. An arbitrary `prototype_migration.rs` was outside that old check. | Modify | Instead of a Git-diff baseline, review-fix slice scans every production Rust source and member/root manifest for SDK2 conductor/runtime markers while requiring the existing ACP 0.10 pre-migration manifest. | Portable across rebases and source archives; adding a production marker made C14 red. Gate: C14 review-fix checkpoint. |
| F5 | Probe-only stale-file scanning lets disposable files elsewhere in `.cyril-41bs` evade cleanup; scan the complete artifact directory. | `DecisionSliceReview` | Verified | Source audit showed the old allowlist walked only `probe.sdk2`; `.cyril-41bs/prototype.tmp` was outside it. | Accept | Review-fix slice compares every file under `.cyril-41bs` with a closed retained-artifact manifest. | A spike-root prototype reported its exact path and made C14 red; restoration green. Gate: C14 review-fix checkpoint. |
| F6 | ADR-0012 keeps memory/source seams while ADR-0003 promised moving them when proxies activate; explicitly supersede the old promise. | `DecisionSliceReview` | Verified | Direct comparison of ADR-0003's persistent-memory exception with ADR-0012's App/source decision exposed contradictory migration triggers despite the old ADR's superseded status. | Accept | Review-fix slice states that ADR-0003's move-on-proxy-activation promise is superseded and that both seams remain for the SDK2 migration. | Documentation contract fix; no production behavior change. Gate: C14 review-fix checkpoint. |
| F7 | A hard-coded Git baseline makes the broadened C14 census fail after rebases, shallow clones, or source archives; remove Git-history dependence. | `DecisionSliceReview` | Verified | The intermediate implementation called `git diff <fixed SHA>` with `check=True`; the SHA/object is not guaranteed in a source archive or rebased/shallow checkout. | Modify | Review-fix slice replaces Git history with a closed artifact allowlist plus production source/manifest content census. | The final fence imports no subprocess/Git API and passes from the working source tree. Gate: C14 review-fix checkpoint. |
| F8 | The fence checks only that `review-decisions.md` exists, so missing/invalid F1–F7 dispositions can pass; validate the decision table. | `ReviewFixPlanGate` | Verified | Direct source audit found `review-decisions.md` only in `required_artifacts`; no content was read or validated. | Accept | Review-fix slice parses the decision rows, requires exactly F1–F13, validates the closed evidence/decision states, and rejects empty evidence/fix/note cells. | Gate: remove F13's row, observe `review_decisions_complete: false`, restore, and rerun C14 green. |
| F9 | The production census scans runtime markers only in Rust sources, so conductor/runtime dependency markers in manifests can evade it; scan manifests too. | `ReviewFixPlanGate` | Verified | Direct source audit found `forbidden_production_markers` applied only to `production_sources`; manifests were checked for one conductor literal only. | Accept | Review-fix slice scans root/member manifests together with production Rust sources and retains the exact ACP 0.10 dependency check. | Gate: add a conductor dependency marker to a member manifest, observe its exact path/marker red, restore, and rerun C14 green. |
| F10 | C14's design approval predicate does not enforce the C14 lifecycle row as PASS; require the discharged status. | `ReviewFixPlanGate` | Verified | Direct source audit found `design_approved` checked only overall APPROVED/requester words. Changing C14's status back to PENDING would not affect that predicate. | Accept | Review-fix slice parses C14's falsification-table status and requires it to start with PASS. | Gate: mutate the C14 status to PENDING, observe `c14_design_status: PENDING`, restore, and rerun C14 green. |
| F11 | No fence predicate protects ADR-0012's explicit supersession of ADR-0003's memory-move promise; add one. | `ReviewFixPlanGate` | Verified | Direct source audit found ADR checks for general supersession and seam retention but not the persistent-memory promise at ADR lines 178–181. | Accept | Review-fix slice requires ADR-0012's explicit statement that the persistent-memory move promise is superseded. | Gate: remove that sentence, observe `supersedes_memory_move_promise: false`, restore, and rerun C14 green. |
| F12 | Slice 2's mutation command is generic rather than exact; replace it with individually executable mutation commands and expected failures. | `ReviewFixPlanGate` | Verified | Direct plan audit found one instruction to “Apply each” mutation rather than exact mutation-specific commands. | Accept | Review-fix slice lists exact backup, mutation, red-fence, and restore commands for every named mutation. | Process-contract fix; checkpointed-build executes every listed mutation before commit. |
| F13 | `design.md` still calls C14 pending in route/I16 text despite its PASS row; discharge all C14 lifecycle references and fence against regression. | `ReviewFixPlanGate` | Verified | Direct read found “C14 owns the pending ADR/cleanup checkpoint” and “Assigned to pending C14” while the C14 table row and summary said PASS/discharged. | Accept | Review-fix slice rewrites both stale references and requires the pending phrases to be absent. | Gate: restore one stale phrase, observe `design_c14_discharged: false`, restore, and rerun C14 green. |
| F14 | Line-based SDK2 dependency detection misses valid multiline Cargo dependency tables; parse TOML dependency tables instead. | `RevisedReviewFixPlan` | Verified | A valid `[dependencies.agent-client-protocol]` table places `version = "2"` on a separate line, so no old line contains both strings required by the intermediate scanner. | Accept | Review-fix slice uses `tomllib` and recursively inspects normal, development, build, workspace, and target dependency tables, including renamed packages. | Gate: add a valid multiline SDK2 dependency table to `cyril-ui`, observe the exact manifest/package/version red, restore, and rerun C14 green. |
| F15 | `SPEC-E3-001`: E3 records but never asserts unknown-first, reversed, and removed-handler outcomes; add a permanent red/green fence. | `FinalStackSpec` | Verified | Direct read of `e3.rs` lines 225–245 found only the slow-handler `ensure!`; all three `CaseResult` values were printed without checking their connection/events contract. | Accept | Final review Slice 3 asserts the exact contained and parse-failure outcomes and makes altered handler order/removal fail the probe. | Gate: E3 probe plus oracle must pass; a wrong expected event mutation must make E3 fail. |
| F16 | `SPEC-E6-001`: conductor live parity checks only three methods per engine; compare the complete direct live contract and report exact divergences. | `FinalStackSpec` | Verified | The original comparator used a three-method subset and omitted lifecycle, provenance, cardinality, failure, identity, and cancellation cells. Exact full live method streams are backend/model-dependent, so treating them as topology output would create a flaky false contract. | Modify | Final review Slice 5 exact-compares stable first-occurrence lifecycle milestones and evidence provenance for two direct plus six conductor sessions, requires every KAS host family/permission/turn-end contract, and names v2 host callbacks N/A. A separate direct/conductor matrix exhaustively checks all 18 requests, two notifications, actual IDs, typed errors, transformed payloads, and exact callback order; `e6` owns cancellation/failure semantics. | Gate: exact cell sets and lifecycle milestones match; removing any deterministic callback or changing IDs/error/transform payload makes the comparator fail with a named divergence. |
| F17 | `SPEC-E5-001`: E5's authenticated live run intentionally avoids tools, so permission and advertised callback families are only synthetic; exercise/record the full engine-specific live contract. | `FinalStackSpec` | Verified | The original `run_live` sent only initialize/new/prompt, while the separate duplex matrix could not by itself prove the authenticated KAS host-callback flow requested by route E5. | Modify | Final review Slice 5 advertises engine-appropriate capabilities, requires KAS to exercise auth/filesystem/terminal/permission/hooks plus structural `turn_end`, names v2 host callbacks as N/A when no tool trigger exists, and separately proves all 18 requests and two notifications in the deterministic direct/conductor matrix. Authenticated live results explicitly mark typed-error, response-ID, and cancellation cells not exercised rather than filling them with plausible defaults. | Gate: authenticated v2/KAS direct runs and all six conductor cells satisfy exact stable lifecycle/provenance contracts; every KAS host family is true; direct and conductor matrices preserve actual observed response IDs, exact typed error data, and transformed payload counts; removing a required transform marker or adding an undeclared marker makes the comparator fail. |
| F18 | Standards: E6 matches terminal failure via debug text; compare structured error code/data. | `FinalStackStandards` | Verified | `e6.rs` line 337 used `format!("{error:?}").contains("agent-crash")`; SDK `Error` publicly exposes typed `code`, `message`, and `data`. | Accept | Final review Slice 4 checks `ErrorCode::InternalError` and exact JSON data while retaining diagnostic serialization only for output. | Gate: E6 probe/oracle green; changing the expected typed data makes the probe fail. |
| F19 | Standards: E3 collapses responsiveness timeout and closed channel with `.ok().flatten()`; distinguish expected timeout from infrastructure failure. | `FinalStackStandards` | Verified | Direct read of `e3.rs` lines 128–132 showed both timeout and `recv() == None` collapse to false. | Accept | Final review Slice 3 matches timeout explicitly and returns a contextual error for closed/unexpected events. | Gate: E3 probe/oracle green; closed-channel control returns an error rather than the intended negative result. |
| F20 | Standards: E6 collapses cancellation timeout/closed channel with `.ok().flatten()`; require the exact cancellation event. | `FinalStackStandards` | Verified | Direct read of `e6.rs` lines 264–267 showed timeout and closure both become `None` while the case still succeeds. | Accept | Final review Slice 4 propagates timeout/closure context and requires the exact terminal cancellation event. | Gate: E6 probe/oracle green; suppressing the event makes the case fail. |
| F21 | Standards: E6 silently skips malformed wire-log JSON through `.ok()?`; parse every relevant entry or return an error. | `FinalStackStandards` | Verified | Both request/response `find_map` closures discarded `serde_json::from_str` failures, allowing later valid evidence to mask corruption. | Accept | Final review Slice 4 parses all direction-matching entries into typed JSON with path/direction context before identity lookup. | Gate: E6 probe/oracle green; an injected malformed wire entry returns a parse error. |
| F22 | Standards: E1's embedded positive Rust control panics on runtime send/receive/join errors; return diagnostics with `Result`. | `FinalStackStandards` | Verified | The committed `good` fixture used three `expect` calls for channel and thread operations. | Accept | Final review Slice 3 changes the embedded control to `Result`, propagates send/receive failures, and maps a panicked actor to `io::Error`. | Gate: E1 oracle compiles/runs the positive control and still rejects the `Rc` negative control. |
| F23 | Standards: E4 uses `expect_err` for an unexpected successful process probe; return a contextual probe error instead of panicking. | `FinalStackStandards` | Verified | `e4.rs` lines 106–111 called `expect_err` on a runtime connection outcome. | Accept | Final review Slice 3 destructures the result, bails on unexpected success, and preserves the actual error on expected failure. | Gate: E4 probe/oracle green; unexpected success returns an `anyhow` error. |
| F24 | Fowler Duplicated Code: E5/E6 live probes duplicate event, credential, and callback helpers; extract one private probe module before expanding the matrix. | `FinalStackStandards` | Verified | Direct comparison found verbatim `Events`, SQLite token loading, and callback mapping in both binaries; the two copies had already drifted in names and outputs. | Modify | Final review Slice 5 extracts a private `src/live_support.rs` module shared through explicit path modules; topology-specific connection logic stays in each binary and no production abstraction is added. | Probe-only duplication with two real callers; gate: both live binaries compile and all direct/conductor cells pass. |
| F25 | Standards: C14 recognizes only SDK 2 dependency requirements beginning with `2`, so valid Cargo requirements such as `=2.0.0`, `^2`, or `~2` evade the production migration fence. | `Cyril41bsFinalStandards3` | Verified | `version.startswith("2")` did not normalize operators and also affected renamed-package table forms. | Accept | C14 now accepts only simple requirements confined to the pre-migration 0.10 line; every other explicit `agent-client-protocol` requirement and every conductor dependency is reported. | Gate: add `agent-client-protocol = { version = "=2.0.0" }` to a member manifest, observe the exact manifest/package/version red, restore, and rerun C14 green. |
| F26 | Standards: the live comparator trusts Rust-emitted lifecycle/host booleans and no longer requires agent-message chunks. | `Cyril41bsFinalStandards3` | Verified | A cell could retain expected normalized milestones and true booleans while its raw event list omitted agent chunks or contradicted terminal ordering. | Accept | The comparator independently normalizes raw events, derives prompt/turn-end/permission/family/chunk facts, cross-checks emitted evidence, and requires at least one agent-message chunk. | Gate: the `missing_agent_message` and `terminal_order` self-tests must fail with named divergences; authenticated direct and conductor cells remain green. |
| F27 | Standards/spec: F25's version-only fix still allows an explicit ACP `git` or `path` dependency with no `version` to evade C14. | `Cyril41bsAcceptanceStandards` / `Cyril41bsAcceptanceSpec` | Verified | The predicate guarded rejection with `version is not None`, so non-workspace source overrides were silently accepted despite the “every other explicit requirement” contract. | Accept | C14 validates the complete dependency spec: only a simple crates.io 0.10 requirement or a source-free inherited workspace dependency is pre-migration; `git`, `path`, missing-version, and other explicit specs are reported. | Gate: add an ACP `path` dependency to a member manifest, observe its exact path/source red, restore, and rerun C14 green. |
| F28 | Standards: an explicit ACP 0.10 requirement from a custom Cargo registry still passes the crates.io-only F27 policy. | `Cyril41bsAcceptanceStandards` | Verified | The complete-spec helper rejected `git` and `path` but did not treat `registry` and related source selectors as overrides. | Accept | The pre-migration allowlist rejects every Cargo source selector (`git`, `path`, `registry`, `registry-index`, `branch`, `tag`, `rev`) before evaluating version/workspace inheritance. | Gate: add an ACP 0.10 custom-registry dependency, observe its manifest red, restore, and rerun C14 green. |
| F29 | Standards: C14's dependency-table traversal skips Cargo `[patch]` and `[replace]` overrides, allowing an ACP source replacement to evade the migration fence. | `Cyril41bsAcceptanceStandards` | Verified | Recursive traversal recognized dependency table names but never interpreted patch source maps or replacement package/version keys as dependency specs. | Accept | The manifest parser now collects normal/dev/build dependencies, every `[patch.<source>]` entry, and `[replace]` entries with normalized package names before applying the same ACP source/version policy. | Gate: add a `[patch.crates-io]` ACP git override, observe its exact manifest/source red, restore, and rerun C14 green. |
