# Raw Request

Requester typed, verbatim:

> cyril-6bol

The requested behavior is the full open tracker record returned by `rivets show cyril-6bol` on 2026-08-01: **“KAS terminal shell_type responder hardcodes \"bash\" (+ create runs argv with no shell) — wrong for native Windows.”**
