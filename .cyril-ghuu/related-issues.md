# Related issues

Tracker search was bounded to theme, palette, semantic color, conversation, chat,
Markdown, syntax, input, and autocomplete.

- `cyril-ghuu` — current conversation-surface migration.
- `cyril-ixua` — closed prerequisite that defines the signed 19-role semantic
  contract and explicit color projections.
- `cyril-nrnq` — sibling migration for modal surfaces.
- `cyril-dij8` — sibling migration for application chrome.
- `cyril-6r3a` — downstream removal of legacy palette access after all migration
  batches land.
- `cyril-fkke` — downstream additional bundled palettes.
- `cyril-qaq0` — downstream configuration and theme selection.
- `cyril-a14l` and `cyril-4vvw` — downstream layout and editing work touching
  chat/input surfaces.

No earlier closed issue independently implements or specifies the conversation
surface migration. `cyril-ixua` explicitly leaves this migration out of scope.
