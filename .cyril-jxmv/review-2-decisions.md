# Post-PR review-feedback decisions (PR #89, review 2)

Two-axis review (standards / spec) ran against `496a250...5c4cf7a` after the PR
opened; 9 raw findings dedup to 7 (two were flagged by both axes). Assessed per
`gilfoyle:assessing-review-feedback`: every bug claim verified before any fix,
every fix evaluated on its own merits, deferrals carry tracker IDs.

| # | Finding (one line) | Reviewer | Category | Verified? | Decision | Note |
|---|---|---|---|---|---|---|
| 1 | In-process C8 fence reads real `CYRIL_AGENT_LOCATION`; a runner exporting `native` flips the asserted Wsl classification | standards + spec | Bug | Yes — reproduced red: `CYRIL_AGENT_LOCATION=native cargo test ... bridge_spawn_binds_agent_location_before_exec` fails `Some(Native) != Some(Wsl)` | **Accept** (child-fence conversion) | Commit `1508be5`. Matches the file's own documented convention ("fences that assert a specific global state run in CHILD processes"); in-process scrubbing is impossible — `env::remove_var` is `unsafe` in Rust 2024, forbidden workspace-wide. Green re-verified plain AND poisoned. |
| 2 | `bind_agent_location` collapses `VarError::NotPresent`/`NotUnicode` with `.ok()` — corrupt override silently degrades, contradicting the fn's own "any other value logs a warning" doc | standards | Bug (minor) | Yes — read `path.rs:100`; non-Unicode never reaches `resolve_agent_location`'s warn arm | **Modify** (fix jxmv site; defer 8tq6 twin) | Commit `80f3582`: match on `VarError`, warn on `NotUnicode`, fall back to detection; unix child fence injects invalid-UTF-8 bytes. The identical pre-existing collapse on `CYRIL_WSL_DISTRO` is NOT this PR's code — deferred, tracked at **cyril-3br3**. |
| 3 | CLAUDE.md Platform Constraints still says "Windows: spawns `wsl kiro-cli acp`", contradicting this PR's new adjacent text | standards + spec | Style/doc | Yes — line present at head; code default confirmed native (`main.rs:27`) | **Duplicate of cyril-duz0** — defer | duz0 AC1 is verbatim this fix (plus transport.rs comment + README); absorbing one line would fragment its 3-AC scope. duz0 is open, P3, ready-for-agent. Same disposition as the pre-PR review. |
| 4 | Location carrier is a process-global `AtomicU8`, not threaded through `AgentCommand` as the issue's design note suggested | spec | Design | Yes — design.md claim 4 explicitly records the decision: "process-global, last-spawn-wins; cyril runs one bridge per process today (`_kiro/workflow` peer sessions ride the same bridge)"; tradeoff also documented at `path.rs:87-89` | **Reject** | Design notes are "suggested", not ACs; once-per-spawn and no-per-call-re-derivation — the notes' actual requirements — are honored. No concurrent-bridge scenario exists or is on the roadmap. |
| 5 | `translate_paths_in_json` is ungated — a future caller would bypass the agent-location gate | spec | Design | Yes — but the helper pre-exists on main (line 286 at merge-base); this PR only added its contract doc | **Reject** | Mechanics layer is uniformly ungated (`win_to_wsl`/`wsl_to_win` are equally pub); policy lives at the typed boundary (`to_native`/`to_agent`), which the shipped KAS-5 fs callbacks (cyril-7bdu, closed) already use. Pre-adding a gated wrapper with zero callers = speculative generality. Doc contract at `path.rs:422-424` states exactly this. |
| 6 | `pub fn set_agent_location` lets any production caller silently disable translation | standards | Design | Yes — pub confirmed; doc marks it a test seam | **Reject** | Visibility is forced by the integration-test crate boundary (`tests/win_wsl_wiring.rs` compiles externally; `#[cfg(test)]` can't help). cyril-core is an internal workspace crate, not a published API; misuse is same-workspace-reviewable, and the gate still requires a Windows host. |
| 7 | C8 fence duplicates bridge.rs's missing-binary/timeout/`BridgeDisconnected` test scaffold | standards | Polish (smell: Duplicated Code, judgement call) | Yes — shape shared with `spawn_failure_disconnect_reason_wellformed` | **Reject** | Cross-compilation-unit (lib unit test vs integration binary); sharing needs a pub test-util or dev-dep crate — wrong-sized for ~15 scaffold lines, and finding 1's fix reshaped the scaffold anyway. |

Post-fix validation: cyril-core 563 plain + 797 `--features kas`, win_wsl_wiring
4/4 (including the two new/converted child fences), `clippy --all-targets
-D warnings` clean on both feature sets, `fmt --check` clean; finding 1's
poisoned-env reproduction re-run green.
