# Review-feedback decisions

| # | Finding | Reviewer | Category | Verified? | Decision | Note |
|---|---|---|---|---|---|---|
| 1 | Preserve approval origin through trust confirmation | DesignFalsifier | Bug/design | Yes. `UiState::approval_confirm` returns only `TrustOption` (`state.rs:1781-1846`), while `App::persist_trust_grant` selects the main session's current mode (`app.rs:698-732`). | Modify | Enrich the confirmation result with `SessionId`; preserve main persistence, block and report foreign-to-main writes. Durable foreign-config persistence is tracked by verified issue `cyril-ufld`. |
| 2 | Generic workspace compilation is not a distinct placement fence | DesignFalsifier | Design | Yes. The workspace can compile after deliberately exposing or relocating queue state, so the original falsifier could pass despite the claimed boundary regressing. | Modify | Claim 8 now uses explicit negative compile operations: external queue reordering must fail privacy, and responder consumption through immutable `TuiState::approval()` must fail ownership. No source-text test is introduced. |
| 3 | Historical findings cite the now-evolved probe/oracle as if they still reproduce the pre-fix output | StandardsReview | Evidence/design | Yes. `findings.md:9-35` names the live paths, while HEAD's oracle requires `VecDeque` and emits five fixed-state lines. | Modify | Pin the historical evidence to immutable prototype commit `955a1a3`; keep HEAD's oracle as the checkpointed-build oracle instead of duplicating executable artifacts. |
| 4 | Main-session approval titles allocate on every frame | StandardsReview | Hard convention | Yes. Both widget `None` arms call `.to_owned()` for unchanged static titles. | Accept | Build the `Span` inside the match: static titles remain borrowed and only attributed titles format an owned string. |
| 5 | Approved queue-boundary negative compile fence is absent | SpecReview | Spec | Yes. No compile-fail fixture or doctest fences private queue mutation and responder consumption through `&ApprovalState`. | Modify | Put two external `compile_fail` doctests on `TuiState::approval`, pinned to E0616 (private field) and E0507 (move through shared reference); this is more local than a standalone harness and runs in existing doctest gates. |
