# Review decisions: cyril-kryv

Sources: `review-issues.md` supplied by the requester and the merge-readiness review of stacked drafts #97–#99. Findings retain stable IDs across both review rounds.

| finding-id | finding | reviewer | evidence-state | evidence | decision | fix | note |
|---|---|---|---|---|---|---|---|
| F1 | Shared sidecar cursor lets retries attribute later turns to earlier records. | review-issues.md | Verified | Direct code reading plus A-fails/A+B-arrive/B-first regression reproduced the ownership hazard. | Modify | Correlate complete sidecar turns to ordered outstanding record IDs, retain per-record pending results, and advance only through the last complete turn. | Gate passed: `late_retry_and_partial_tail_preserve_kas_turn_ownership`; full default/KAS integration passed. |
| F2 | Inline account RPC blocks the bridge mediator. | review-issues.md | Verified | The command arm awaited `ext_method` inline; a parked fake RPC demonstrated the mediator boundary. | Modify | Spawn the account request on the bridge `LocalSet`, enforce a five-second timeout, and emit one terminal notification. | Gate passed: `slow_account_query_does_not_block_bridge_loop`; full default/KAS integration passed. |
| F3 | `/usage` sends the account query before showing local SQLite state. | review-issues.md | Verified | Command execution awaited the bridge send before returning `ShowUsage`; a dropped command receiver made local open fail. | Modify | Return local `ShowUsage` from the command, open the panel in App, then best-effort `try_send`; send failure becomes typed unavailable state. | Gates passed: `kas_usage_marks_account_query_for_app_dispatch` and `usage_account_query_order_and_state_matrix`; full integration passed. |
| F4 | Account percentage above 100 is rejected. | review-issues.md | Verified | `required_percentage` rejected `>100`; a 104% account fixture hit the branch. | Modify | Accept finite nonnegative percentages and clamp domain/display percentage to 100 while preserving usage and overage values. | Gate passed: `account_usage_response_maps_exactly`; full integration passed. |
| F5 | Success with an explanatory message but no data discards the message. | review-issues.md | Verified | Success branch required `data` and never read `message`. | Modify | Convert missing/null data with a nonempty response message into the typed query-failure path carrying the backend explanation. | Gate passed: admin-managed row in `account_usage_response_maps_exactly`; full integration passed. |
| F6 | `UsagePanelState::row_count` disagrees with rendered Costs/Context rows. | review-issues.md | Verified | Direct row-model comparison showed sparse Costs rendered three rows while reporting one, and Context reported twelve for one/six/ten lines. | Modify | Count the exact fixed/model/account rows and derive Context count from latest/breakdown presence. | Gate passed: `sparse_cost_and_context_row_counts_match_rendered_lines`; full integration passed. |
| F7 | Open usage panels do not refresh after turn/context writes and enrichment redraw is not requested. | review-issues.md | Verified | App refreshed only after successful enrichment and did not set `redraw_needed`. | Modify | Centralize refresh from durable SQLite after turn, context, and enrichment writes and set redraw without resetting page/scroll. | Gate passed: `open_usage_panel_refreshes_on_turn_and_context_writes`; full integration passed. |
| F8 | Account parser drops `hasLimit`, `addOnCredits`, and `overageCapable`. | review-issues.md | Verified | Current live capture fields were absent from domain/parser types. | Modify | Model and validate all three fields, including active/expiry add-on data; render no-cap plans without a fake quota. | Gates passed: captured `account_usage_response_maps_exactly` and `kiro_full_mixed_pages_render_at_floor`; full integration passed. |
| F9 | Account overage charges, enterprise marker, and resource type are not rendered. | review-issues.md | Verified | Values existed in `UsageAccount` but `cost_lines` omitted them. | Modify | Render enterprise plan state, resource type, actual overage charge, and distinguish `Account <display name>` from local per-turn credits. | Gate passed: `kiro_full_mixed_pages_render_at_floor`; full integration passed. |
| F10 | Failed enrichments are requeued forever and block the FIFO worker. | review-issues.md; merge-readiness review | Verified | The original App cleanup omitted worker ownership; the red/green abandoned-A→B fence proved a stale ID captures B without an explicit worker abandon command. | Modify | Retain ownership after the first retryable failure, but send an idempotent worker `Abandon` command on terminal/exhausted failure; remove exact pending/outstanding state without discarding unowned later turns. | Gates passed: `enrichment_retry_budget_allows_one_transient_retry_only`, `late_retry_and_partial_tail_preserve_kas_turn_ownership`, and `abandoned_record_does_not_capture_later_turn`; disabling abandon turned the last fence red, restored green. |
| F11 | Tools page drops token-share and monetary-cost columns. | review-issues.md | Verified | `ToolUsageGroup` retained both values while `tool_lines` rendered neither. | Accept | Restore token share and `format_costs` alongside detailed credits/chars/last-used fields. | Gate passed: `kiro_full_mixed_pages_render_at_floor`; full integration passed. |
| F12 | Account overage charges, enterprise state, and resource type are omitted. | review-issues.md | Verified | Same renderer omission as F9. | Accept | Covered by the atomic F9 account-detail renderer fix. | Gate passed: `kiro_full_mixed_pages_render_at_floor`; full integration passed. |
| F13 | Bridge disconnect leaves account status loading/refreshing forever. | review-issues.md | Verified | `BridgeDisconnected` cleared live turn/context state but not account query status. | Modify | Retain the last-known account and set status to `Unavailable` with the disconnect reason. | Gate passed: `usage_account_state_retains_last_known_with_status`; full integration passed. |
| F14 | Cache savings renders from literal `None` and falsely claims backend gating. | review-issues.md | Verified | No cache-savings source exists in core; renderer passed literal `None`. | Modify | Remove the unsourced row and update the exact Overview row count. | Gate passed: usage renderer/state tests; full integration passed. |
| F15 | `Stale("refreshing")` uses a sentinel error string for active refresh. | review-issues.md | Verified | `mark_usage_account_loading` encoded refresh as a `Stale` message. | Modify | Add explicit `UsageAccountStatus::Refreshing`; reserve `Stale` for query failures. | Gate passed: `usage_account_state_retains_last_known_with_status`; full integration passed. |
| F16 | Engine-neutral source fence is cross-crate, package-fragile, and trivially evaded. | review-issues.md | Verified | Core test `include_str!` referenced a UI source path and searched incomplete substrings. | Modify | Remove the cross-crate source scan and replace C13 with byte-identical provider-invariance rendering plus existing crate/module ownership boundaries. | Gate passed: `overview_metric_availability_is_provider_invariant`; design/plan amended; full integration passed. |
| F17 | No exact account-response wire fixture exists. | review-issues.md | Refuted | `terminal-aborted-2.16.2.jsonl` and `kas-rpc-sweep-2.16.0.jsonl` contain exact responses; parser test reads the committed fixture. | Reject | N/A — existing fixture and parser fence already cover the claim. | Permanent no-op: duplicate evidence, not deferred work. |
| F18 | Full live v2-and-KAS acceptance is unavailable due credentials. | review-issues.md | Refuted | Prior KAS acceptance passed; live v2 session `d831ff76-f27a-43e5-877a-7d115cb46718` on `gpt-5.6-terra` returned exact `OK`, then `/usage` showed six successful turns, one provider request, zero retries, typed credits, context, folder/model grouping, and backend-gated token/cost fields for v2 records. | Reject | N/A — the reported credential blocker is gone; no code change was required. | Gate passed: both real KAS and v2 TUI/bridge paths have live evidence. |

| F19 | The v1→v2 migration classifies every legacy row without an error string as successful, including cancelled and other non-success stop reasons. | merge-readiness review | Verified | Direct SQL/code comparison plus the red/green legacy outcome matrix: the error-only mutation produced five successes instead of one. | Modify | Derive migrated outcome with error precedence, then cancelled, then clean end-turn success, with every other terminal reason classified as error. | Gate passed: `v1_migration_is_lossless_idempotent_and_enrichment_atomic`; default/KAS workspace tests, strict Clippy, and rustfmt passed. |
| F20 | `TurnMeteringUpdated.used_tools` must be copied into portable durable tool rows. | merge-readiness review | Refuted | Direct domain/routing check: the summary contains vendor-specific names but no call IDs or portable kinds; durable portable calls come from typed ACP `ToolCallStarted`/`ToolCallUpdated`, while C1 already fences exact preservation in the metering notification. | Reject | N/A — no sound name-to-kind or call-ID mapping exists at this layer. | Permanent no-op: inventing IDs/kinds would fabricate per-call attribution and can double-count the authoritative ACP lifecycle. |
| F21 | Invalid context percentages can be clamped or persisted as fabricated aggregates. | merge-readiness review | Verified | The approved spec rejects invalid percentages; Kiro/KAS adapters accepted raw finite out-of-range values, `ContextUsage::new(NaN)` retains NaN, and `UsageLog::record_context` lacked a defensive range check. | Modify | Preserve the established clamped constructor for trusted/session calculations, add strict adapter validation for untrusted wire values, and reject invalid values at persistence. | Gate passed: strict constructor/adapter tests, `record_context_rejects_invalid_direct_percentages`, `context_snapshot_rejects_corrupt_percentages`, and full default/KAS integration. |
| F22 | KAS context bucket percentages are persisted without finite or 0–100 validation. | merge-readiness review | Verified | `parse_bucket` called infallible `ContextBucket::new`; SQLite bucket columns and row conversion had no range check. | Modify | Add a fallible bucket constructor, reject malformed buckets at conversion, and validate again at persistence/read boundaries. | Gate passed: strict bucket/adapter tests, invalid write/read fences, and full default/KAS integration. |
| F23 | One enrichment read can merge multiple appended sidecar turns into the first durable record. | merge-readiness review | Verified | Both parsers consumed the full tail and stopped only after aggregating every assistant/result or usage-summary frame; the cursor then advanced to EOF. | Modify | Stream and return exactly one complete turn plus its consumed byte boundary, leaving later turns for later record IDs. | Gate passed: `streamed_turn_reader_preserves_v2_and_kas_boundaries`; removing the prompt boundary made the fence red, restored green; full integration passed. |
| F24 | Empty or partial sidecar success deletes valid portable tool rows. | merge-readiness review | Verified | Parsers could return `Ok([])` after a weak completion marker and `UsageLog::enrich_record` unconditionally deleted every existing row before inserting the sidecar subset. | Modify | Parse complete turn boundaries strictly and merge exact sidecar rows by call ID without deleting portable-only calls. | Gate passed: `enrichment_merges_partial_and_empty_sidecar_tools`, partial-tail sidecar fence, and full integration. |
| F25 | Aborting a failed prompt dispatch leaves a context identity that can persist a phantom turn. | merge-readiness review | Verified | `begin_turn` inserted `contexts`; `abort_turn` removed only `pending`; the single-context fallback then accepted a later global context frame. | Modify | Remove the aborted turn's context identity atomically with its pending turn. | Gate passed: `abort_turn_removes_context_identity_from_global_frames`; removing cleanup made the fence red, restored green; full integration passed. |
| F26 | A compaction completion without a recorded start fabricates a measurable compaction. | merge-readiness review | Verified | The `Completed` arm used `or_insert` with the current percentage, and the next sample emitted a reduction despite no start boundary. | Modify | Ignore and warn on completion without an existing started compaction. | Gate passed: `compaction_completed_without_started_is_ignored` plus corrupt read-boundary and full integration fences. |
| F27 | Session IDs reach sidecar path construction and metadata probing before validation. | merge-readiness review | Verified | `SessionId::new` is unvalidated and `WorkerCommand::SessionStarted` called `locate_jsonl` before `validate_session_id`. | Modify | Validate before every path construction/probe and return a typed terminal failure. | Gate passed: `sidecar_baseline_rejects_loaded_missing_and_invalid_sessions` and full integration. |
| F28 | A loaded session whose sidecar is absent at startup can later ingest its full historical file from offset zero. | merge-readiness review | Verified | Every locate/metadata failure stored cursor zero; loaded and fresh origins were not represented in the worker command. | Modify | Carry `SessionOrigin`; allow zero only for a fresh missing file and fail closed for a loaded missing/unavailable baseline. | Gate passed: fresh-versus-loaded matrix in `sidecar_baseline_rejects_loaded_missing_and_invalid_sessions` and full integration. |
| F29 | Three sidecar attempts can exceed the specified one-second elapsed budget. | merge-readiness review | Verified | Retry sleeps totaled 750 ms but blocking reads/parsing had no elapsed deadline and terminal errors were retried. | Modify | Track a one-second deadline through attempts/parsing and stop immediately on terminal failures. | Gate passed: `bounded_current_turn_enrichment_matrix` elapsed/terminal rows and full integration. |
| F30 | Sidecar files can grow past 64 MiB after the metadata check and be read without a hard cap. | merge-readiness review | Verified | `metadata().len()` preceded uncapped `read_to_string`/whole-file reads, leaving a concurrent-growth allocation race. | Modify | Read through a 64 MiB plus one-byte sentinel cap and reject overflow without advancing the cursor. | Gate passed: over-cap reader row in `bounded_current_turn_enrichment_matrix` and full integration. |
| F31 | KAS sidecar parsing buffers the complete appended tail instead of O(longest-line) memory. | merge-readiness review | Verified | `read_appended` materialized one `String` up to the file cap before line parsing, contrary to the approved streamed-JSONL decision. | Modify | Parse capped complete lines incrementally and retain only current-turn state. | Gate passed: streamed v2/KAS multi-turn and partial-line fence plus full integration. |
| F32 | A Kiro turn without an optional metering notification never schedules sidecar enrichment. | merge-readiness review | Verified | `PendingTurn.sidecar_kind` began `None` and was set only by v2/KAS metering; App enriched only `Some`, allowing the next turn to inherit the skipped tail. | Modify | Bind the configured sidecar kind when the turn begins, independent of optional metering frames. | Gate passed: `no_metering_turn_retains_configured_sidecar_kind` and full integration. |
| F33 | Sidecar parse errors report synthetic labels instead of the actual failing path. | merge-readiness review | Verified | v2/KAS JSON errors embedded constant pseudo-paths and App had no other path context. | Modify | Thread the validated real path into every parser/error. | Gate passed: malformed complete-line path assertion in `bounded_current_turn_enrichment_matrix` and full integration. |
| F34 | The required 100,000-turn snapshot fence is disabled in normal validation. | merge-readiness review | Verified | The deterministic in-memory test carried `#[ignore]`; a direct ignored run passed its functional and two-second assertions on the reference workstation. | Accept | Remove `#[ignore]` so normal default/KAS suites enforce C12. | Gate passed: `kiro_snapshot_remains_bounded_at_100k` ran in both normal full workspace suites; default/KAS integration passed. |
| F35 | KAS sidecar data supplies a billed model that must override requested identity. | merge-readiness review | Refuted | Live KAS `messages.jsonl` has no billed provider/model and `session.json` exposes only configured `modelId: auto`; only v2 has the established billed-model path. | Reject | N/A — retain `None` until wire or sidecar evidence exposes billed identity. | Permanent no-op: labeling configured `auto` as billed would fabricate identity. |
| F36 | A Prompt-only V2 cancelled/error turn is merged with the following prompt and captures its tools. | merge-readiness review | Verified | The prior parser split on a later Prompt only when the preceding segment already parsed as complete; `Prompt(A), Prompt(B), Assistant(B)` therefore produced one A-owned tool segment. | Modify | Treat every later Prompt as a boundary, emit a closed Prompt-only prefix as an empty turn, retain an incomplete final Prompt/tail, and advance only through the last closed/complete line. | Gate passed: `streamed_turn_reader_preserves_v2_and_kas_boundaries`; removing the Prompt boundary produced two tools in the first turn, restored green. |

| F37 | PR #97 targets a Phase 1 feature branch whose four implementation commits are absent from `main`. | merge-readiness review | Verified | Git ancestry shows `feat/cyril-gfkm-usage-observer` ahead four/behind two versus `main`; the two main-only commits change Rivets metadata, and no merged PR contains the Phase 1 code. | Modify | Open a separate Phase 1 PR to `main`, merge it first, then retarget #97→#98→#99 serially so each existing increment retains its review-sized diff. | Gate passed: PR #100 is open, merge-clean, and green across all 12 CI jobs; its four-commit/28-file Phase 1 diff remains separate from #97. |

## Review-fix checkpoint

1. **Affected unit tests — PASS.** Every focused fence named above passed after restoration.
2. **PENDING falsifiers — N/A — reason:** C5/C9/C10 were discharged by their committed owning slices; amended C13 is `PASS` in `design.md`.
3. **Stress fixture — PASS.** Legacy outcomes, invalid context/bucket rows, 100,000 records, fresh/loaded/traversal sidecars, two-turn v2/KAS tails, Prompt-only/partial lines, cap/deadline failures, retained late retries, and abandoned-A→B ownership all matched their literal matrices.
4. **Implementation vs oracle — PASS.** Raw SQL outcomes/context/tool rows, one-turn byte offsets, record ownership sets, captured account fields, exact rendered row counts, and byte-identical provider-invariance buffers agreed with independent oracles.
5. **Production budgets — PASS.** Sidecar reads were streamed and capped at 64 MiB, internal retries stayed within one second, the normal 100,000-record snapshot fence passed, and the 10,000-entry account/10,000-group render reference gates passed.
6. **Regression fences — PASS.** Slices 7–11 migration, context, sidecar, bridge, command/App, parser, state, row-count, render, refresh, retry/abandon, scale, and provider-invariance fences are green.
7. **Named mutations — PASS (red).** Error-only migration produced five successes; removing abort context cleanup persisted a phantom turn; removing the V2 Prompt boundary gave A two tools; disabling worker abandon made B fail; the original account/modal/provider mutations also turned their owning fences red.
8. **Fence restoration — PASS.** Every mutation was restored and its focused fence reran green.

Final integration: `cargo test` passed 1,429 tests; two consecutive `cargo test --features kas` runs passed 1,737 tests each; default/KAS Clippy with `-D warnings` and rustfmt check passed. Live KAS and v2 acceptance both passed: the v2 `gpt-5.6-terra` turn returned exact `OK`, and `/usage` rendered outcomes, provider requests/retries, typed credits, context, model/folder grouping, and backend-gated v2 token/cost fields.
