# Review-feedback decisions

Review range: `origin/main...HEAD` for Rivets issue `cyril-ixua`.

| # | Finding | Category | Verified? | Decision | Note |
| ---: | --- | --- | --- | --- | --- |
| 1 | ANSI nearest-color helpers use plausible palette indices as empty-iterator defaults | Style / convention | Yes. `unwrap_or(16)`, `map_or(0, ...)`, and probe `unwrap_or(0)` conflict with CLAUDE.md's “Errors are not default values” rule even though current candidate sets are non-empty. | Modify | Fold from an explicit first candidate so non-emptiness is structural and no fallback value can hide a future empty range. |
| 2 | The semantic-theme initiative is absent from a numbered roadmap phase | Style / convention | Yes. CLAUDE.md says non-trivial work should land in a numbered phase, and `docs/ROADMAP.md` has no theme entry. | Accept | Add a numbered vendor-neutral client feature entry that records the seam and its activation/migration follow-ups. |
| 3 | The fixed 19-role inventory is duplicated across source, resolved, projection, and test shapes | Design / smell | Yes, but the repetition enforces the approved fixed-field completeness boundary and keeps the contract fence independent from projection code. | Reject | A generating macro or shared iterator would couple implementation and oracle while making the public contract less explicit; the current bounded 19-role shape is intentional. |
| 4 | Public role field names do not preserve the signed semantic vocabulary | Spec | No. The signed `.cyril-ixua/probe-output.tsv` and independent oracle use `chrome`, `text`, `accent`, `accent_alt`, and the other implemented identifiers exactly; the prose table uses display labels, not mandated Rust field spellings. | Reject | Renaming would drift from the executable signed artifact rather than repair a spec violation. |
| 5 | `CONTEXT.md` orchestration glossary additions are unrelated to the theme seam | Scope | Yes. The added context-usage and crew vocabulary is unrelated to `cyril-ixua`. | Accept | Revert those additions from this branch. |
| 6 | The Windows-only `Duration` cfg fix is outside the theme specification | Duplicate of `cyril-gu8a` | Yes, but it is a separately committed and closed prerequisite discovered when the required Windows strict-Clippy gate failed. | Reject | Keep the isolated prerequisite commit: without `cyril-gu8a`, `cyril-ixua` cannot satisfy its full-workspace quality-gate acceptance criterion on Windows. |
