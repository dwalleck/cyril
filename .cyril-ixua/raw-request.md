# Raw request

Verbatim requester statements from the planning conversation that produced
`cyril-ixua`:

> "I think we should have some themes beyond those three, but probably not
> arbitrary themes"

- "yes" — in response to modeling visual theme and terminal color mode
  separately.
- "yes" — in response to the six-theme initial bundle.
- "yes" — in response to automatic color-capability detection, explicit
  overrides, and `NO_COLOR` support.
- "yes" — in response to including a live `/theme` picker.
- "yes" — in response to live preview with rollback on cancel.

Current invocation:

> "cyril-ixua"
