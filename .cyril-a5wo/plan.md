# Plan: cyril-a5wo

## Inputs

- Route: Empirical (`.cyril-a5wo/route.md`).
- Approved design: `.cyril-a5wo/design.md`; requester approval recorded verbatim on 2026-08-18; no risk acceptances.
- Evidence: `.cyril-a5wo/evidence.md`, `.cyril-a5wo/probe-wire-raw-input.py`, six committed live captures, and the prior live probe/oracle artifacts.
- Cheapest falsifier: design C1 is `PASS`; every other design row is assigned below to the slice that creates and mutates its permanent fence.

## Review budget and PR increments

| Slice | Projected changed lines |
|---|---:|
| Slice 1 — pinned fixture and provenance | 1,250 |
| Slice 2 — absent/partial display fence | 160 |
| Slice 3 — six-capture recovery fence | 190 |
| **Slice sum** | **1,600** |
| Churn margin | **400 (25%)** |
| **Projected total** | **2,000** |

The 25% margin covers fixture-shape adjustments, Rust assertion diagnostics, and the already-committed 583-line empirical capture/probe base. The projected total is below the 4,000-line review-size gate.

Single PR increment: **a5wo-interruption-fences** (Slices 1–3). Mergeable definition: source provenance is pinned, the source-derived fixture deserializes through the ACP schema, both display shapes pass through the existing core test adapter, and all six cancel captures converge in `UiState`. It verifies without any later increment because there is no later increment.

## Slice 1: Pin source-derived absent and partial wire fixtures

**Claim IDs:** C1, C2

**Expected behavior:** The pinned `@kiro/agent` 0.38.7 source audit fails closed on drift. A deterministic four-frame JSONL fixture contains: absent `rawInput` on an initial `user_input` call and its status-only completion; path-only partial `rawInput` on an initial streaming replace call; and path plus partial `newStr` on its update. ACP deserialization preserves those exact option branches.

**Oracle:** The independent AST enumeration and hand-counted source trace in `.cyril-a5wo/evidence.md`: emitter boundary lines 459232–459250, `user_input` lines 488440–488450, partial streaming-action lines 429385–429396 and 445879–445899, and adapter forwarding lines 488864–488971.

**Stress fixture:** The partial path is absolute and contains an embedded space plus Unicode (`/workspace/src/space ü.rs`). Expected: fixture bytes regenerate exactly; ACP preserves the path string and the absent/partial `rawInput` branches without normalization or coercion.

**Regression fence:** `crates/cyril-core/src/protocol/convert/mod.rs::source_derived_raw_input_fixture_is_exact`, created in this slice, plus `.cyril-a5wo/derive-raw-input-fixture.py --check` byte comparison.

**Named mutation:** C1 — change one digit of `EXPECTED_SHA256` in `.cyril-a5wo/probe-wire-raw-input.py`; the probe must exit nonzero. C2 — delete `rawInput` from the partial initial fixture frame; the exact-shape test and generator byte check must fail.

**Complexity/production scale:** The source probe and generator each scan $B$ source bytes once: $O(B)$ time and $O(B)$ memory, with current production source $B=20.7$ MB. Explicit accepted maximum: 2 seconds per one-off command on the project workstation; rationale: the existing probe completes in 0.21 seconds, so 2 seconds is a conservative drift allowance. The Rust fence scans four fixture frames: $O(4)$ time and bounded fixture memory.

**Wall budget/phase:** N/A — source audit, fixture derivation, and tests are one-off build phases; no production runtime phase is introduced.

**Files:**
- `.cyril-a5wo/route.md`
- `.cyril-a5wo/evidence.md`
- `.cyril-a5wo/probe-wire-raw-input.py`
- `.cyril-a5wo/design.md`
- `.cyril-a5wo/plan.md`
- `.cyril-a5wo/derive-raw-input-fixture.py` (new)
- `crates/cyril-core/tests/fixtures/kas/tool_call_raw_input_2_18_1_source_derived.jsonl` (new)
- `crates/cyril-core/tests/fixtures/kas/README.md`
- `crates/cyril-core/src/protocol/convert/mod.rs`

**Estimate:** 45 minutes

**Diff estimate:** 1,250 changed lines, including prior committed empirical artifacts and all Gilfoyle artifacts.

**PR increment:** a5wo-interruption-fences

**Commands and expected results:**
- `./.cyril-a5wo/probe-wire-raw-input.py <pinned-acp-server.js> <pinned-package.json>` → package 0.38.7 and source SHA match; four initial and fifteen update sites agree with the AST oracle; final verdict is `REPRESENTABLE absent=true partial=true`. Under the C1 hash mutation it exits nonzero; restored source gate passes.
- `./.cyril-a5wo/derive-raw-input-fixture.py --source <pinned-acp-server.js> --check crates/cyril-core/tests/fixtures/kas/tool_call_raw_input_2_18_1_source_derived.jsonl` → all cited source markers match and generated bytes equal the committed fixture; output reports the recorded fixture SHA. Under the C2 missing-`rawInput` mutation it exits nonzero; restored fixture passes.
- `cargo test -p cyril-core --features kas source_derived_raw_input_fixture_is_exact` → four frames deserialize; absent initial/update and path-only/expanding partial shapes equal the source oracle. The C2 mutation fails with the frame-specific shape assertion.

## Slice 2: Fence converted absent and partial input display behavior

**Claim IDs:** C3, C6

**Expected behavior:** Replaying the source-derived fixture through `cyril_core::test_support::kas_capture_to_routed` and `UiState::apply_notification` commits two tracked calls. The absent call returns `None` from both display helpers. The partial replace call returns `/workspace/src/space ü.rs` from `primary_path` and `None` from `command_text`. No panic occurs. UI tests consume domain notifications only; ACP parsing remains inside core.

**Oracle:** Direct expected-value table derived independently from the pinned source: `user_input` carries no `rawInput`, location, or command; streaming replace carries one path location and no `command` member. The compiler and manifests independently enforce that `cyril-ui` has no direct `agent-client-protocol` dependency.

**Stress fixture:** Same absent-input branch and Unicode/space path from Slice 1. Expected: total helper behavior for `None`, byte-identical path return for partial input, and no fabricated command.

**Regression fence:** `crates/cyril-ui/src/state.rs::source_derived_absent_and_partial_raw_input_display_safely`, created in this slice; compiler fence from `cargo test -p cyril-ui --no-run`.

**Named mutation:** C3 — replace the optional `raw_input().and_then(...)` path with `raw_input().expect("input")`; the absent case must panic. C6 — add `use agent_client_protocol as acp;` to the UI test without a forbidden direct dependency; the compiler must reject the unresolved import.

**Complexity/production scale:** Test-only loops parse four fixture lines and apply four notifications: $O(F+N)$ for fixture bytes $F<4$ KB and $N=4$. Explicit accepted maximum: 500 ms for the focused test; rationale: four bounded frames exercise no I/O after compile-time inclusion. No production loop changes.

**Wall budget/phase:** N/A — one-off test phase; no production runtime phase is introduced.

**Files:**
- `crates/cyril-ui/Cargo.toml`
- `crates/cyril-ui/src/state.rs`

**Estimate:** 30 minutes

**Diff estimate:** 160 changed lines.

**PR increment:** a5wo-interruption-fences

**Commands and expected results:**
- `cargo test -p cyril-ui source_derived_absent_and_partial_raw_input_display_safely` → exactly two source-derived calls; absent helpers return `None`/`None`; partial helpers return exact Unicode path/`None`. Under the C3 mutation the test panics; restored code passes.
- `cargo test -p cyril-ui --no-run` → UI test target compiles through core's dev-only `kas` + `test-support` features without a direct ACP dependency. Under the C6 import mutation compilation fails unresolved import; restored code compiles.

## Slice 3: Fence all six cancel-recovery lifecycles

**Claim IDs:** C4, C5

**Expected behavior:** Each named 2.16.2 and 2.18.1 capture is unwrapped to its recorded JSON-RPC frames, converted through the production KAS adapter, and applied to a fresh `UiState`. At the initial, middle, and terminal checkpoints, one captured identifier owns one committed message. Guarded fields retain the initial snapshot when omitted. After `failed` then cancelled `turn_end`, the committed status is `Failed`, active calls are empty, and activity is `Ready`.

**Oracle:** `.cyril-a5wo/oracle-subagent-interrupt.py` independently groups raw JSON frames by exact `toolCallId`; its `recovery_frames` rows and `.cyril-a5wo/findings.md` record `pending` → `in_progress` → `failed` followed by cancelled `turn_end` in all six captures. Initial raw JSON is the independent expected field snapshot.

**Stress fixture:** Six distinct session/tool-call identities across two engine generations; middle update omits title and kind; terminal update carries empty-string raw output; unrelated metering/context frames surround the lifecycle. Expected: every named capture independently converges to the same state without ID sampling, duplicate commit, field erasure, or stuck activity.

**Regression fence:** `crates/cyril-ui/src/state.rs::cancellation_captures_converge_to_one_failed_committed_call`, covering all six captures; existing core fences `merge_update_preserves_content_when_update_has_none`, `merge_update_preserves_title_when_update_is_empty`, `merge_update_preserves_raw_input_when_update_has_none`, and `ledger_partial_update_preserves_non_empty_fields`.

**Named mutation:** C4 — make the `ToolCallUpdated` arm append a message instead of updating `tool_call_index`; committed count becomes three and the new fence fails. C5 — replace guarded title/raw-input/content/location assignments in `ToolCall::merge_update` with unconditional assignments; the update checkpoint or existing guard fences fail.

**Complexity/production scale:** Test-only replay is $O(C+B+N)$ for $C=6$ captures, total bytes $B<400$ KB, and fewer than 250 recorder rows. Explicit accepted maximum: 2 seconds for the focused lifecycle test; rationale: bounded in-memory JSON parsing and state application only. No production algorithm changes.

**Wall budget/phase:** N/A — one-off test phase; no production runtime phase is introduced.

**Files:**
- `crates/cyril-core/src/test_support.rs`
- `crates/cyril-ui/src/state.rs`

**Estimate:** 45 minutes

**Diff estimate:** 190 changed lines.

**PR increment:** a5wo-interruption-fences

**Commands and expected results:**
- `cargo test -p cyril-ui cancellation_captures_converge_to_one_failed_committed_call` → six named captures each report one committed failed call, exact checkpoint field equality, zero active calls, and Ready activity. Under the C4 append mutation every capture fails its committed-count assertion; restored code passes.
- `cargo test -p cyril-core merge_update_preserves_` → title, content, raw-input, and raw-output omission guards stay green; the C5 unconditional-assignment mutation turns the relevant guards red; restored code passes.
- `cargo test -p cyril-core ledger_partial_update_preserves_non_empty_fields` → session-scoped ledger keeps non-empty title/input/content/locations; the C5 mutation turns the fence red; restored code passes.

## Checkpointed-build results

### Slice 1 — PASS (`12b40fc`)

1. Affected unit/integration tests — **PASS**: focused core fixture test plus `cargo test` (1,370 passed).
2. Pending falsifiers — **PASS**: C1 source gate reverified; C2 exact fixture fence established.
3. Stress fixture — **PASS**: absolute path with embedded space and Unicode survived byte regeneration and ACP conversion exactly.
4. Implementation vs independent oracle — **PASS**: lexical probe and independent AST enumeration agreed on 4 initial sites, 15 update sites, absence, and the three pinned path-only producers.
5. Loop budget — **PASS**: source probe 0.23 seconds and generator 0.21 seconds, each below the 2-second one-off budget; four-frame Rust replay reported 0.00 seconds.
6. Always-on wall budget — **N/A — reason**: no production runtime phase or loop changed.
7. Regression fences — **PASS**: source hash/site gate, generator byte equality, and exact ACP fixture-shape test green.
8. Named mutations and restoration — **PASS**: C1 hash mutation exited nonzero; C2 missing-`rawInput` mutation failed both byte and shape fences; restored commands green.

Post-commit drift/size: fetched `origin/main`; no upstream commits to review; cumulative diff 1,297 changed lines, below the 2,000-line review tripwire.

### Slice 2 — PASS (`1684877`)

1. Affected unit/integration tests — **PASS**: focused display fence, UI `--no-run`, workspace tests (1,670 passed), and clippy.
2. Pending falsifiers — **PASS**: C3 display-totality and C6 core/UI dependency boundary both established.
3. Stress fixture — **PASS**: absent input and `/workspace/src/space ü.rs` produced `None`/exact-path/`None` without panic or fabricated command.
4. Implementation vs independent oracle — **PASS**: display values matched the pinned source-derived table; compiler independently rejected a UI-side ACP import.
5. Loop budget — **PASS**: four-frame in-memory replay reported 0.00 seconds, below 500 ms.
6. Always-on wall budget — **N/A — reason**: test-only dependency features and assertions add no production phase.
7. Regression fences — **PASS**: focused display and UI compilation fences green.
8. Named mutations and restoration — **PASS**: `primary_path` `expect` mutation panicked; direct ACP import failed unresolved; restored focused and compilation fences green.

Post-commit drift/size: fetched `origin/main`; no upstream commits to review; cumulative diff 1,338 changed lines, below the 2,000-line review tripwire.

### Slice 3 — PASS (`e2c3922`)

1. Affected unit/integration tests — **PASS**: six-capture lifecycle fence, four merge guards, ledger guard, workspace tests (1,671 passed), and clippy.
2. Pending falsifiers — **PASS**: C4 convergence and C5 per-update field preservation both established.
3. Stress fixture — **PASS**: six captures, two KAS generations, six IDs, surrounding context/metering frames, omitted middle fields, and terminal empty output all converged independently.
4. Implementation vs independent oracle — **PASS**: production-path replay matched the Python oracle's per-ID `pending → in_progress → failed` ordering and recorded cancelled turn end.
5. Loop budget — **PASS**: six-capture in-memory replay reported 0.00 seconds, below 2 seconds.
6. Always-on wall budget — **N/A — reason**: recorder adaptation and replay exist only behind test-support.
7. Regression fences — **PASS**: each update checkpoint retained title/kind/input/content/locations; final state had one failed committed call, zero active calls, and `Activity::Ready`.
8. Named mutations and restoration — **PASS**: append-on-update produced three committed calls; unconditional merge assignments erased the title at the first captured update and failed three merge guards plus the ledger guard; all restored fences green.

Post-commit drift/size: fetched `origin/main`; no upstream commits to review; cumulative implementation diff 1,463 changed lines, below both the 2,000-line review tripwire and 4,000-line hard gate.

### Assembled implementation gate — PASS

- Source/provenance: pinned package 0.38.7 and SHA matched; source probe reported `REPRESENTABLE absent=true partial=true`; generated four-frame fixture matched SHA `384dc37487079a49ac1f344648c32850cd2e81bced44165d6a6cfa86fe03719b`.
- Focused fences: source fixture 1 passed; display 1 passed; six-capture lifecycle 1 passed; merge guards 4 passed; ledger guard 1 passed; UI `--no-run` passed.
- Workspace: two consecutive clean `cargo test` runs each passed 1,671 tests across 19 suites with 5 ignored; `cargo clippy -- -D warnings` passed; `cargo fmt --all --check` passed.
- Stability note: the first assembled workspace run intermittently observed an empty tracing capture in unrelated existing `malformed_workflow_field_matrix_isolated`; its exact test, the full KAS core suite, and both subsequent workspace runs passed without source changes.
- Independent final review: **PASS** — reviewer reported no actionable correctness, acceptance, boundary, or fence findings.
- Final drift/size: **PASS** — final fetch found no `origin/main` movement; assembled review surface is 1,523 changed lines, below both thresholds.
- Gate state: every applicable item is **PASS**; every non-applicable wall-budget item is recorded `N/A — reason`; no checkpoint has a `FAIL`.

## Tracker taxonomy

- Permanent non-goals remain exactly those approved in `design.md`: no recursive JSON completeness merge, duplicate-start redesign, late-after-turn reordering, display-precedence redesign, or malformed/non-object expansion. These are settled scope decisions, not deferred work.
- Intended future work remains covered by verified existing issues: `cyril-ebqu` for KAS agent-subtask rendering/grouping and `cyril-2yn8` for trusted tool identity/raw-input documentation. No new tracker issue is required.

## Self-review

- [x] C1–C6 are assigned exactly once; every design claim is `PASS`.
- [x] Every slice has all thirteen mandatory fields; every conditional field has an explicit `N/A — reason` where applicable.
- [x] Each claim's fence and named mutation land in the same slice.
- [x] Every new loop records asymptotic cost, bounded test/source size, and an explicit accepted maximum; no always-on production phase is introduced.
- [x] Review arithmetic includes a documented 25% margin and yields one independently mergeable increment below 4,000 lines.
- [x] Deferrals are classified under the tracker taxonomy with verified IDs where future work exists.
- [x] Checkpointed-build completed all slices; every gate item is `PASS` or `N/A — reason`, with no `FAIL`.
