# cyril-cc5e — Budgeted plan

Design: `.cyril-cc5e/design.md` (approved; cheapest falsifier passed 57,400/0).
Claim coverage: C1,C2,C6,C7→S2 · C3,C4→S3 · C5,C10→S4 · C8,C10(arith)→S1 ·
C9→every slice's verification (existing state tests must pass unmodified).

Global conventions: expected drawn-sets in fences are **hardcoded literals
generated by the Python oracle** (`oracle-v2.py`), never recomputed by the
Rust formula inside the test — keeps the fence's oracle independent of the
implementation. All new arithmetic is saturating. No state files are touched
in any slice.

---

## Slice 1: `modal::centered` shared geometry helper

**Claim:** C8 (geometry parity with the old inline arithmetic), C10-partial
(degenerate areas never panic).
**Oracle:** the old formula from `picker.rs:8-14` transcribed verbatim into
the test (from git history), evaluated side-by-side; plus hand-computed
literals for the odd-remainder cases.
**Stress fixture:** area 0×0; area smaller than desired (desired-width 80 on
area 20×8 → clamp); odd remainder centering (area.w=61, width=56 → margins
2 and 3, asymmetry ≤1) — expected Rects written as literals before coding.
**Loop budget:** parity sweep test loop: O(|areas| × |desired|) = 49 × 36 ≈
1.8×10³ pure-arithmetic ops, test-only. Production: `centered` is O(1) per
frame. No budget risk.
**Wall budget:** n/a (not always-on; O(1) per render frame).
**Files:** `crates/cyril-ui/src/widgets/modal.rs` (new),
`crates/cyril-ui/src/widgets/mod.rs` (register module).

**Code (advisory):**
```rust
/// Centered popup rect: clamps desired size to `area` minus a 4-cell margin
/// pair, then centers. Total function — degenerate areas yield empty Rects.
pub fn centered(area: Rect, desired_width: u16, desired_height: u16) -> Rect {
    let width = desired_width.min(area.width.saturating_sub(4));
    let height = desired_height.min(area.height.saturating_sub(4));
    let x = area.x + area.width.saturating_sub(width) / 2;
    let y = area.y + area.height.saturating_sub(height) / 2;
    Rect::new(x, y, width, height)
}
```
Note: picker does NOT switch to the helper in this slice — parity is proven
first, the consumer flips in S2 as part of the render rewrite.

**Verification:**
- [ ] Unit tests pass (`modal::tests`, incl. `centered_parity_sweep`)
- [ ] Stress fixture produces expected literals (0×0, clamp, odd-remainder)
- [ ] prove-it oracle still agrees (old picker untouched → probe outputs identical)
- [ ] Budgets hold (arithmetic-only)
- [ ] `cargo test -p cyril-ui` + workspace clippy/fmt gates

## Slice 2: picker windowing core

**Claim:** C1 (selection always visible), C2 (contiguous exact fill), C6
(no-overflow layout unchanged), C7 (empty result safe).
**Oracle:** `oracle-v2.py` (new-model Python arithmetic, extends
`window-model-check.py`) generates SCENARIO literals; fences assert those
literals plus formula-free properties (▸ marker present, selected label
present, drawn labels contiguous). C6's oracle is the **pre-change probe
output committed at d8cf13f** (scenario D drawn-set must be preserved for
fitting lists).
**Stress fixture:** tail-clamp: n=20, k=19 @ 80×24 → expected drawn
opt-05..opt-19 (an unclamped `start = k − rows/2` would index out of bounds
or leave blank tail rows); n=16/k=15 (one past the cap, window pinned to
tail); n=1/k=0; n=0 (empty — usize underflow candidate); duplicate labels
(two options share a label; assert exactly one ▸ row — catches fences that
"find" the selection by label match alone).
**Loop budget:** render: O(rows) ≤ 15 lines per frame (production; replaces
the old O(visible) — unchanged asymptotics). Fence sweep: n∈{1,3,7,15,16,20,
30,100} × k∈{0,mid,last} × sizes{60×16, 80×24, 200×50} = 72 renders ×
O(w×h ≤ 10⁴ cells) ≈ 7×10⁵ ops, test-only, under 10⁶.
**Wall budget:** n/a (per-keypress render, O(15) lines).
**Files:** `crates/cyril-ui/src/widgets/picker.rs` (rewrite render with the
design formula incl. `desc_reserve`; consume `modal::centered`),
`crates/cyril-ui/tests/picker_viewport.rs` (new: `selection_always_visible`,
`window_contiguous_fill`, `small_list_layout_unchanged`,
`empty_filter_no_panic`).

**Code (advisory):** the design's core formula, verbatim from design.md §
Core formula; window slice `filtered_indices[start..start+rows]`; desc line
inserted after the selected row inside the window.

**Verification:**
- [ ] Unit tests pass (4 new fences + existing `picker_renders`)
- [ ] Stress fixtures produce pre-written expected outcomes
- [ ] `oracle-v2.py` output matches the probe scenarios re-rendered on the new binary (A/B/D preserved semantics, B now `marker=true` with drawn window from oracle-v2)
- [ ] Existing `state::tests` pass unmodified (C9)
- [ ] Budgets hold; workspace gates green

## Slice 3: description reserve fences + overflow scrollbar

**Claim:** C3 (description contained, height stable across k), C4 (scrollbar
iff overflow, display-only).
**Oracle:** height read from the rendered border glyphs (top/bottom corner
row indexes), compared to a hand-computed literal per fixture; scrollbar
presence = ratatui scrollbar glyph in the right border column vs the
arithmetic `n > rows` computed by oracle-v2 literals; display-only = `git
diff --stat` of `crates/cyril/src/` is empty (no key-path change).
**Stress fixture:** mixed descriptions (selected option has NONE while
others have Some → row still reserved, height identical to all-desc case —
catches reserve-if-selected jitter bug); all-None (reserve 0 — no wasted
row, D-scenario height preserved); n=rows exactly (NO scrollbar — boundary)
vs n=rows+1 (scrollbar).
**Loop budget:** height-stability fence renders all k for n=20 @ 80×24: 20
renders × 10⁴ ≈ 2×10⁵ ops, test-only. Scrollbar render: O(1) widget per
frame.
**Wall budget:** n/a.
**Files:** `crates/cyril-ui/src/widgets/picker.rs` (scrollbar),
`crates/cyril-ui/tests/picker_viewport.rs`
(`description_contained_height_stable`, `scrollbar_iff_overflow`).

**Verification:**
- [ ] Unit tests pass (2 new fences)
- [ ] Stress fixtures (mixed-desc jitter, exact-fit boundary) pass as pre-written
- [ ] oracle-v2 agreement holds on re-run
- [ ] `crates/cyril/src/` diff empty (display-only proof)
- [ ] Budgets hold; workspace gates green

## Slice 4: floor walk + degenerate fences + oracle close-out

**Claim:** C5 (60×16 full navigation keeps selection visible and advances),
C10 (below-floor sizes render without panic).
**Oracle:** per-step expected windows for the 15-option walk generated as
literals by `oracle-v2.py --walk` (Python, no Rust); "advances" = the ▸ row's
label differs from the previous step's (buffer observation, no formula).
**Stress fixture:** the walk itself is the adversarial input (probe scenario
C was invisible at k=14; the fence walks THROUGH k=14). Degenerate renders at
20×8, 5×5, 0×0 (saturation underflow candidates). Expected: every step
marker-visible; degenerate = no panic.
**Loop budget:** 15 steps × render 60×16 (≈10³ cells) ≈ 1.4×10⁴ ops; 3
degenerate renders. Test-only, trivial.
**Wall budget:** n/a.
**Files:** `crates/cyril-ui/tests/picker_viewport.rs`
(`floor_60x16_full_walk`, `degenerate_sizes_no_panic`),
`.cyril-cc5e/oracle-v2.py` (+ its committed output).

**Verification:**
- [ ] Unit tests pass (2 new fences)
- [ ] Walk matches oracle-v2 literals step-by-step; degenerate = no panic
- [ ] Final probe-vs-oracle-v2 agreement recorded in `.cyril-cc5e/` (audit)
- [ ] Full workspace gate: `cargo test`, clippy `-D warnings`, `cargo fmt --check`, doctests
- [ ] Budgets hold

---

## Plan Self-Review

1. **Loops:** S1 parity sweep 1.8×10³ (test); S2 render O(rows≤15)/frame
   (production, unchanged asymptotics) + sweep 7×10⁵ (test); S3 stability
   fence 2×10⁵ (test); S4 walk 1.4×10⁴ (test). All stated, all under 10⁶,
   none always-on. **No gaps.**
2. **Fixtures:** S1 degenerate/clamp/odd-remainder (rounding bugs); S2
   tail-clamp (unclamped-center bug), n=0 (underflow), duplicate labels
   (label-match false positive); S3 mixed-desc (reserve-if-selected jitter
   bug), exact-fit boundary (off-by-one scrollbar); S4 walk-through-k=14
   (the probe's own failing case), below-floor sizes. All adversarial, all
   with pre-written expectations. **No gaps.**
3. **Doc-comment preconditions:** `modal::centered` — none (total function).
   Picker render documents "out-of-range `selected` renders no marker" —
   classified sanity-hint (state machine maintains the invariant,
   probe-proven; violation yields a marker-less frame, not wrong data) →
   defensive `.get()` indexing, no `debug_assert!` needed, degenerate fence
   covers no-panic. **No load-bearing precondition ships unenforced.**
4. **Write targets:** render writes to the ratatui `Frame` (UI, n/a);
   fences print SCENARIO diff lines to test stdout (data for the oracle
   diff, intentional); no new file or stderr writes. **No gaps.**
5. **Tracker references:** cyril-nrnq (colors), cyril-a14l / cyril-uw20
   (other modals adopt helper), cyril-lxuo (row content) — all verified open
   in this session's tracker scans. No new deferrals introduced by the plan.
   **No gaps.**
